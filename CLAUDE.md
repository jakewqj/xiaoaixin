# 小爱心 · 项目宪法

> 这是一个爸爸做给 6 岁女儿童童玩的电子宠物网页。
> 主角是一只雌性儒艮(dugong dugon),名字叫\*\*小爱心\*\*。
> 每次开始工作前请完整读一遍本文件,并严格遵守。

\---

## 一、三条不可违背的设计原则

### 1\. 没有失败态

小爱心**永远不会死、不会生病、不会因为长时间没人来而衰弱**。

* 禁止:掉血、生命值、饥饿惩罚、连续登录中断、限时任务、倒计时压力
* 允许:状态变化(饿了 / 困了 / 想你了),但状态永远可逆,且不会导致任何损失
* 玩家离开一周后回来,小爱心的反应是「我好想你」,不是「你抛弃了我」

**理由**:成年人的养成游戏用愧疚感驱动留存。对 6 岁孩子这样做是有害的。

### 2\. 文字是奖励,不是门槛

童童已能轻度识字并有对话理解能力,所以文字要用,但要用对。

* 每句话 **3–8 个字**,大字号
* **不做拼音。** 中文下方那行小字的位置留给英文(见第九节第三层),没有对应英文就空着
* 不用成语、不用书面语、不用生僻字
* **底线**:任何一句话读不懂,只看图标也能完成操作。文字永远是「加分项」,不是「必要条件」

### 3\. 不打扰

* 换气机制只在 **session 内**触发(小爱心游上水面吐泡泡 → 点它帮它换气)
* **禁止任何后台推送通知、桌面通知、声音提醒**
* 不做「每日签到」「连续天数中断即清零」这类机制

\---

## 二、绝对禁止清单

写代码前先自查,以下内容一律不准加入,即使你认为「游戏性更好」:

* ❌ 任何形式的付费、内购、广告、虚拟货币充值
* ❌ 任何账号系统、登录、注册、手机号、邮箱收集
* ❌ 任何后端服务器、数据库、用户数据上传
* ❌ 任何社交功能、排行榜、好友、聊天室
* ❌ 死亡、流血、暴力、恐怖、黑暗惊吓元素
* ❌ 「不要告诉爸爸妈妈」类的秘密叙事
* ❌ 弹窗式的强制引导教程

**如果你觉得某个功能很棒但可能触碰以上任何一条,先停下来问我,不要直接写。**

\---

## 三、技术约束

|项目|规定|
|-|-|
|框架|Vite + React + Tailwind CSS|
|语言|TypeScript|
|动画|CSS transform / transition 优先;复杂时用 Framer Motion。**不引入 Canvas 游戏引擎**|
|状态|React useState / useReducer。**不引入 Redux / Zustand 等状态库**|
|存档|~~localStorage 单个 JSON key:`xiaoaixin\_save`~~ **【已作废,见十四、双存储】**|
|路由|**不使用路由库**。页面切换用状态变量控制|
|后端|**无**。零服务器、零 API(第 6 周的 AI 对话除外,届时另行讨论)|
|部署|Cloudflare Pages,静态构建|
|屏幕|~~手机/平板**竖屏优先**~~ **【已作废,见十二、屏幕与渲染:横屏锁定】**|

**依赖包能不装就不装。** 每次想 `npm install` 新东西之前,先问我。

\---

## 四、视觉规范

### 配色(不要自行更换)

```
--water-shallow  #7FD1D8   浅滩水面
--water-mid      #2E8B9B   中层海水
--water-deep     #14495C   深水区
--sand           #E8DCC0   沙底
--seagrass       #6FA84B   海草
--seagrass-young #A8CC72   新芽海草
--dugong-body    #8CA3B0   小爱心身体
--dugong-belly   #C3D3D9   小爱心腹部
--heart          #FF8FA3   爱心/情感强调色
--ink            #243642   文字
```

### 字体

* 标题/对话:**站酷快乐体**(圆润,适合儿童)
* 正文/拼音:**霞鹜文楷 LXGW WenKai**(识字友好,笔画清晰)
* 两者均可免费商用,用 CDN 引入或本地打包

### 排版底线

* 可点击区域最小 **56×56 px**(儿童手指精度低于成人)
* 对话文字最小 **24px**
* 遵守 `prefers-reduced-motion`
* 键盘焦点可见

\---

## 五、美术素材是可替换资源

**当前使用的是占位 SVG,后期会替换成童童的手绘稿。**

因此:

* 代码中**永远通过 `/assets/pet/` 下的文件路径引用形象**,不要把图形直接内联写死在组件里
* 泡泡、海草等特效的发射位置**必须读 `pet.json` 的锚点**,不许在组件里硬编码坐标
* 替换手稿时应当做到:**只往文件夹里拖 4 个 PNG,不改一行代码**

素材规格见 `assets/pet/pet.json`。

\---

## 六、协作方式

* **每次只做一件事。** 我给你一个任务,你做完就停,不要顺手加功能
* 做完后告诉我**怎么在浏览器里验证**,以及**你改了哪些文件**
* 不确定的地方**先问,不要猜**
* 不要写注释解释显而易见的代码,但**每个游戏机制的核心函数上方写一句中文说明它的意图**
* 遇到你想加但本文件没提到的东西 → 问我

\---

## 七、当前进度

* \[ ] 第 0 周:规范 + 占位素材 + 童童的 5 句口头禅
* \[ ] 第 1 周:它活了(一片海 + 会漂浮的小爱心 + 点击反应)
* \[ ] 第 2 周:换气 + 饱食度 + 剧本对话 + 存档
* \[ ] 第 3 周:海草床
* \[x] 第 4 周:成长 + 相册 + **手稿替换仪式**
  * 实际做了什么:成长阶段写在 `pet.json` 的「成长」里(小时候 / 长大一点 / 长大了),按累计打开天数走,只改体型和体色,不新增任何图。长大那次会说一句「我长大一点了」,并且一定给一张关于长大的知识卡。相册记的是第一次:见面、换气、喂海草、种海草、海草长成、手稿替换,加上一排成长照片;只显示发生过的,没有总数也没有未解锁格子。手稿替换仪式 = 把 `pet.json` 的「手稿.已替换」改成 true,小爱心下次打开会认出「这是你画的我」,记进相册,以后不再提。`bio_calf_color`(小时候颜色浅)仍是「已核对: false」,所以体色变化做了但这句话还没让小爱心说 —— 等你核对完再打开。
* \[ ] 第 5 周:打磨 + PWA + 上线
* \[ ] 第 6 周:图鉴(可选)/ AI 对话(可选,须先满足安全条件)

每完成一周,把对应项打勾并在下方追加一行「实际做了什么」。

**「周」编号到第 4 周为止。第 5 周之后项目改用 `ROADMAP.md` 的 Season 编号(S0.5、S1…),下面接着记。**

* \[x] S0.5:换皮(横屏锁定 + 480×270 像素分辨率 + 九层图层 + Sprite 帧动画 + 换气镜头摇移 + 新 HUD + 全部迁移到新布局)
  * 实际做了什么:七个子步骤全部做完,逐条状态见 `ROADMAP.md` S0.5 表格。世界九层还是纯色占位,没接真美术(切参考图 or 写生图 prompt 都还没做,是刻意先跳过的)。小爱心已经换成真的像素精灵(`draw-pet.mjs` 程序化生成,不是 SVG 占位了)。过程中发现并修了几个坑:对话气泡+选项和底部按钮在 270px 高的舞台里会挤在一起(把选项挪到台词气泡上方一起显示解决)、小爱心用 `top:62%→70%` 腾出头顶显示气泡的空间。
* \[x] S1:第一个朋友(地点系统 + NPC 框架 + Dolly(英语)+ 送礼 + 熟悉度 + 控制后台 v1)
  * 实际做了什么:地点系统(横向移动 + 镜头跟随 + 未开放地点模糊轮廓)、NPC 组件(读 `npc.json`,当前只有 Dolly)、对话系统 v2(按 `语言` 字段切换,非中文台词自动朗读 + 可重复点击朗读,用 Web Speech API,失败静默降级)。选中选项后如果 NPC 有 `happy` 动画会切过去演一下反应。熟悉度系统:存档新增 `familiarity`(邻居 id → 0-5 等级,只增不减)和 `familiarityLastAt`(每个邻居每天最多涨一级,防止连点刷到封顶)。聊完一次(选中一个选项)才涨级,上限读 `config.json` 的「开放NPC」。话题库按等级解锁:等级到了第 3/5 档,`话题库` 里对应档位的话就加进抽取池,但更早解锁的档位不会被替换掉,是累加不是替换。送礼:用户指定「用海草做礼物」——已长成的海草(和喂食共用同一份数量,送礼不消耗、不扣减)当礼物,NPC 旁边多一个 56px 的 🌱 按钮,只有「有长成的海草」且「今天还没送过」时才出现(存档新增 `giftedAt` 记录)。反馈从 `礼物反馈.很喜欢`/`礼物反馈.谢谢你` 随机 50/50 选一档(没填的 NPC 有兜底文案),不做偏好匹配——没有拒绝没有讨厌。送礼和聊天共用同一条「熟悉度每天最多涨一级」的规则,不叠加。控制后台:长按 HUD 左上角小字「小爱心」5 秒 → 4 位数字键盘密码(不用系统键盘,防止误触发)→ 进入可视化设置页,能改 `config.json` 全部字段(开放海域/地点/NPC、系统开关、语言设置、成长设置、知识卡屏蔽、陪伴设置、皮肤、密码、对话日志上限)。改动存 `xiaoaixin_config`,和默认值分层合并,不改文件本身。「恢复默认」清空这层覆盖;「一键回到只有小爱心」把开放地点收回家海草床+换气水面、开放NPC清空。对话日志:每次在 NpcDialogue 里选中一个真的选项(送礼反馈不算)就记一条「时间/NPC/选了什么」到 `xiaoaixin_log`,超过上限丢最旧的,控制后台里能看列表、能导出 txt。
  * 顺手做的、不在 ROADMAP 明确要求里但确认过的事:按用户要求把喂食/种海草按钮从大卡片改成和相册/图鉴同款的 56px 圆按钮;HUD 道具栏的贝壳/礼物两格(对应系统还没做)改成灰态 + 点击提示「长大了才有」,海草那格改成种出来的数量只读展示(不再和喂食按钮功能重复)。**用户明确说过这批按钮"还是很大很难看",但要求先不改、留到以后统一做 UI 设计的阶段——不要在没被要求的情况下去碰按钮尺寸。**

**这一整段工作(S0.5 全部 + S1 全部)截至目前还没有 git commit 过,只在工作区。**

* [x] S0.5+S1 · UI 统一重做(2026-07-30):按参考图重做了 HUD/按钮区/对话面板,顺带修了对话互斥的坑和一个意外发现的成长阶段回归 bug。详见 `ROADMAP.md` 对应新章节和本文件「二十、UI 统一重做」。这批改动同样还没有 git commit。
* [x] S0.5+S1 · 真美术落地(2026-07-30,同日第二轮):小爱心按参考图重画(暖灰褐+吻部肉垫+双前鳍,`draw-pet.mjs` 重写,锚点已更新,儒艮大婶自动继承);世界九层从纯色占位换成 `draw-world.mjs` 程序化生成的像素素材(天空/小岛/水面白沫/远景剪影/8 种珊瑚岩草/金沙海底);Book/Album/SeaPicker/KnowledgeCard/LocationNav/送礼按钮统一成木框+羊皮纸;实测修掉 Dolly 与按钮区重叠(NPC 抬到中上层水域)。浏览器 CDP 实测 8 个状态零报错。详见 `ROADMAP.md`「真美术落地」章节。仍未 git commit。

---
 
## 八、学习伴侣定位
 
小爱心除了是宠物,还是童童探索**数学、自然、生物、历史、百科**的伙伴。
 
### 原则 4:小爱心不是老师,答错没有任何后果
 
这是**不可违背原则**,与前三条同级。
 
- ❌ 禁止:分数、正确率、进度条、星级、错题本、「再想想哦」这类失望语气
- ❌ 禁止:对/错使用不同音效(**答对和答错的音效必须完全一样**)
- ❌ 禁止:任何形式的「测验」「闯关」「答题」界面
- ✅ 小爱心对任何回答的反应都是:「我们一起看看」「我也这么想过」
**理由**:一旦小爱心开始考童童,它就不再是宠物了。孩子对宠物和对老师的感情是两回事,这个项目最珍贵的资产是前者。
 
### 原则 5:知识走白名单,不允许现场生成
 
- 所有知识内容**必须**来自 `content/knowledge.json`
- 第 6 周若接入 AI:知识类问题**只能从白名单里选**,不允许模型自由发挥
- 模型不知道时,必须回答「这个我也不知道,我们去问爸爸」
- **「我不知道」是一个必须实现的功能,不是失败**
**理由**:6 岁孩子没有事实核查能力,而且她会完全相信小爱心。模型编一句错的知识,成本是童童记错三年。
 
---
 
## 九、语言分三层
 
### 第一层:中文短句(主力)
3–8 字,大字号,可切拼音。规则见原则 2。
 
### 第二层:成语(只用字面即义的)
 
- 只允许使用 `content/whitelist.json` 中 `成语` 列表里的词
- 判定标准:**不需要讲典故就能懂**
- ✅ 一模一样、五颜六色、小心翼翼、自言自语
- ❌ 守株待兔、掩耳盗铃、一心一意(前两个要讲故事,第三个太抽象)
- **不要新增成语。想加先问我。**
### 第三层:英语(只做「同一件事的第二种叫法」)
 
- 只允许使用 `content/whitelist.json` 中 `英语` 列表里的词句
- 出现方式:中文旁边的小字副标题,或打招呼时的一句话
- ~~❌ **禁止做任何英语学习模块、英语关卡、单词卡、发音测评**~~ **【原表述已作废,见十五、原则 8:语言是角色属性,不是玩家任务——三条底线不变,换了个位置守】**
- ❌ 禁止出现中英对照表格式的界面
**示例**:
```
早上打开        → 小爱心说「早上好」,下方小字 "Good morning!"
喂食按钮        → 图标 + 「海草」,下方小字 "Seagrass"
换气泡泡冒出时   → 「泡泡!」/ "Bubble!"
```
 
**理由**:零学习压力、零额外界面,她在游戏里自然见到第二遍第三遍。做成「英语学习关卡」这个设计就废了。
 
---
 
## 十、学习是图层,不是模块
 
**禁止新增「学习」页签或学习模式。** 五个学科全部长在已有机制上:
 
| 学科 | 挂载点 | 内容 |
|---|---|---|
| 数学 | 海草床 | 数海草(计数)、5 格饱食度(分数雏形)、海草长成要 3 天(时间)、身体左右对称 |
| 自然 | 海洋环境 | 水深与光线、白天黑夜、潮汐涨落 |
| 生物 | 小爱心自己 | 用肺呼吸不是鱼、是哺乳动物、海草是开花植物不是海藻 |
| 历史 | 图鉴 | 美人鱼传说的由来、人和儒艮的关系 |
| 百科 | 路过的邻居 | 海龟、魟鱼等的知识卡 |
 
### 触发规则
 
- 小爱心**偶尔**主动说一句 —— **不是每次**。建议同一张卡的再次触发间隔至少 24 小时
- 或童童**主动点击**场景中的物体时给一张卡
- ❌ **绝不用弹窗打断**。知识卡以气泡或侧滑卡片呈现,随时可以不看
- ❌ 不要在卡片后追加「你学会了吗」类的追问
---
 
## 十一、进度调整
 
- **图鉴系统从第 6 周提前到第 3 周**,与海草床一起做(数学和生物两块天然长在那里)
- 第 6 周的可选项改为:AI 对话(须满足全部安全条件)/ 更多知识卡内容
 
# 【追加章节 v2】直接粘贴到 CLAUDE.md 末尾

> 章节号接着原文件的「七、当前进度」和之前追加的「八～十一」。
> 粘贴后请把原文件里三处旧规定划掉，见本文末尾「需要作废的旧条款」。

\---

## 十二、屏幕与渲染

|项|规定|
|-|-|
|方向|**横屏锁定**。竖屏时显示「请横过来」提示图，不渲染游戏|
|逻辑分辨率|**480 × 270**（16:9）。所有游戏坐标基于此，不是 CSS 像素|
|缩放|**整数倍**（×2 ×3 ×4）。取 `floor(min(vw/480, vh/270))`，居中，四周留纯色边|
|像素渲染|全局 `image-rendering: pixelated`。**canvas 元素也要加，别漏**|
|镜头|只做垂直摇移（换气）和水平跟随（移动）。**不做缩放**|

**绝不用非整数缩放去填满屏幕。** 像素画被 1.37 倍拉伸就毁了，宁可留黑边。

### 图层结构（9 层，z-index 固定）

```
z:0  天空 + 云（缓慢横移）
z:1  远景小岛
z:2  水面线（波纹动画）
z:3  水下远景（深色剪影植物）
z:4  水下中景（珊瑚、海草，摇摆）
z:5  沙底 + 散落物
z:6  小爱心 + NPC
z:7  气泡粒子
z:8  HUD
```

\---

## 十三、像素美术规范

|项|规定|
|-|-|
|精灵尺寸|小爱心 192×64（逻辑分辨率下,成年基准,按参考图比例定,见 `pet.json` _尺寸依据）。NPC 见各自 `npc.json`|
|调色板|单个精灵**限制在 16 色以内**|
|格式|PNG，透明背景，**无抗锯齿**|
|朝向|一律朝右|
|精灵表|横向排列，代码按帧宽切分|
|帧率|角色精灵 **8 fps**；海草/珊瑚 8 fps；水面 8 fps；气泡为连续粒子|
|帧动画实现|CSS `steps()` + `background-position`。**不引入 canvas 游戏循环**|
|镜头运动|平滑 300–500ms，是唯一允许的连续运动|

**像素风的顿挫感来自低帧率。不要为了"更流畅"提高精灵帧率。**

### 素材是可替换资源（此条继承并强化）

* 代码**永远通过 `/assets/` 下的文件路径引用形象**，不内联图形
* 气泡、海草等特效发射位置**必须读锚点配置**，不许硬编码坐标
* 小爱心支持皮肤切换：`config.json` 的 `"小爱心皮肤": "pixel" | "tongtong"`

\---

## 十四、双存储（重要，容易出事）

原规定「存档 = localStorage 单个 JSON key」**已作废**。现在是双存储：

|存什么|存哪|key|
|-|-|-|
|游戏状态（成长、熟悉度、贝币、解锁进度…）|**localStorage**|`xiaoaixin\_save`|
|后台配置覆盖层|**localStorage**|`xiaoaixin\_config`|
|对话日志|**localStorage**|`xiaoaixin\_log`（上限 500 条，超出丢最旧）|
|**童童的画和手写笔迹**|**IndexedDB**|store: `xiaoaixin\_drawings`|

### 硬性禁止

> ❌ \*\*绝对禁止把 base64 图片写进 localStorage。\*\*
> localStorage 配额约 5MB，第 20 张画就会撑爆，且爆掉时是\*\*整个存档一起丢\*\*。

* 图片一律存 IndexedDB，value 为 PNG blob，key 为 `drawing\_{timestamp}`
* `xiaoaixin\_save` 里**只存图片 id 的引用**
* 所有 IndexedDB 操作必须有 try/catch，失败时静默降级（**绝不白屏，孩子看不懂 error**）

\---

## 十五、原则 6–10

### 原则 6：世界会变化，但变化永远可逆、永远不惩罚

* 海草床没人管 → 长得慢、水浑一点，**不会死光**
* **熟悉度只增不减，永不衰减**。朋友很久没见，第一句是「你去哪儿了呀」

### 原则 7：野生动物不被拥有

所有 NPC 都是自由的邻居。**禁止实现捕捉、饲养、繁殖、圈养机制。**

### 原则 8：语言是角色的属性，不是玩家的任务

* 英语 NPC 就是说英语的，像邻居叔叔说粤语
* ❌ 无单词卡、无发音评分、无正确率
* ❌ **不需要键盘打字**，全部是图标 + 英文短句选项，点击朗读
* ✅ 选"错"了对方只歪歪头再说一遍，**音效完全一样**

### 原则 9：所有生物与场景细节必须有真实出处

每个 NPC、地点、知识卡都要有 `factRef` 指向 `content/facts.json`。
**没有出处的设定不许进游戏。** 你不许自己编"可爱设定"。

### 原则 10：她画的、写的，永远不被判对错，永远不消失

* ❌ **不做手写识别**
* ❌ 不打分、不纠正、不"再写一次"
* ✅ 笔迹原样存图，永久留在世界里

\---

## 十六、新增禁止清单

在原「二、绝对禁止清单」之外，以下同样一律不准加入：

* ❌ 战斗、追逐、逃跑、被吓
* ❌ 捕捉 / 圈养 / 繁殖野生动物
* ❌ 好感度衰减、朋友生气、被讨厌
* ❌ 体力条、饥饿惩罚、时间压力
* ❌ 讨价还价、价格波动、借贷、负数金钱
* ❌ 死亡、灭绝、濒危等字眼出现在童童能看到的文本里
* ❌ 婚恋系统
* ❌ 排行、评级、段位
* ❌ 需要键盘打字的交互
* ❌ 对童童手写/绘画作品的识别、评分或纠正
* ❌ 常驻金钱 HUD（贝币只在「换一换」界面内显示）
* ❌ 超过 4 格的道具栏

\---

## 十七、老虎（虎鲨）安全护栏 —— 不可配置

老虎是唯一一个带专属安全约束的 NPC。**以下每一条都写死在代码里，不做成 config 项，不许通过任何配置绕过：**

* ❌ **永不追逐**。移动速度恒定，方向永远是远离玩家
* ❌ **永不扣任何数值**。无伤害、无惊吓值、无掉落物品
* ❌ **永不发出突然音效**。无低频嗡鸣、无鼓点、无音乐变调
* ❌ **永不出现牙齿特写、张嘴、血、追逐镜头**
* ❌ **永不在夜间出现**
* ❌ **永不"偶遇"**。必须玩家主动前往深水通道才出现
* ✅ **随时可退**。点「退回去」立即回安全水域，无过渡动画
* ✅ 关系类型为 `距离`，上限 3 级。**第 4 级永久锁定**，点击显示「他不会。」

**唯一允许配置的是他存不存在**：`config.json` 的 `"虎鲨": true|false`，**默认 false**。

\---

## 十八、需要作废的旧条款

粘贴本文件后，请把 CLAUDE.md 里以下三处标记为【已作废】：

|位置|旧内容|现在看哪|
|-|-|-|
|三、技术约束 · 屏幕|「手机/平板竖屏优先」|第十二章：**横屏锁定**|
|三、技术约束 · 存档|「localStorage 单个 JSON key」|第十四章：**双存储**|
|九、语言分三层 · 第三层|「禁止做任何英语学习模块…」的原表述|第十五章原则 8：**语言是角色属性**|

第十五章原则 8 **不是**放宽了英语限制。它换了个位置守同一条线：英语是社交事实，不是学习任务。三条底线（不打字、有朗读、音效相同）一条都不能少。

\---

## 十九、协作方式（补充）

原「六、协作方式」继续有效，另加三条：

1. **每个 Season 只做一个新系统。** 我发一条 prompt 你做一件事，做完停下。不要顺手把下一条也做了。
2. **需要角色/场景细节时，我会明确告诉你去读 `docs/GDD.md` 的哪一节。** 不要自己去通读整份企划然后一次性实现一堆东西。
3. **改动涉及本文件任何一条规定时，先停下来问我。** 不要自己修订宪法。

---

## 二十、UI 统一重做（2026-07-30 起）——美术还原度优先

这次「处理已知遗留问题 + UI 设计」的会话里，我明确说过：**接下来整体美术设计的目标是以 95% 还原度做出参考图（`wiki/graphic/stardew参考/`）那样的效果，如果和本文件已有的美术类数值规则冲突，以这个新目标为准。**

受影响、明确改掉的旧规则：

* **§三 排版底线「可点击区域最小 56×56 px」**：这条是老的、按真实设备 CSS 像素写的规则。S0.5 换成 480×270 固定逻辑分辨率 + 整数倍缩放后，同样的数字放在逻辑像素里，缩放后（iPad 常见 ×2）实际触摸区域变成 112px，比预期大很多——这正是"按钮偏大偏丑"的根源。这次 `ActionBar.tsx` 的方形按钮改成了 36 逻辑 px（`h-9 w-9`），不再套 56 这个数。这条规则本身还留着（不是作废，其他还没做美术重做的地方仍按旧的来），但美术统一重做覆盖到的组件以视觉还原度优先。
* **§四 配色「不要自行更换」**：对话面板（见 S1 遗留问题重做）会引入一套新的木纹/棕色调（仿参考图的对话框边框），作为在原有 10 色之外的**追加**，不删改原有那一套海洋配色。新增的变量会在对应组件改动时列出。

**没有变的**：原则 1-10（没有失败态、不打分、知识白名单等）、虎鲨护栏、双存储、九层图层、24px 对话文字最小值——这些不是"美术数值"，这次的优先级调整不覆盖它们。


