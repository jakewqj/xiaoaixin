# 小爱心 · 项目宪法

> 这是一个爸爸做给 6 岁女儿童童玩的电子宠物网页。
> 主角是一只雌性儒艮(dugong dugon),名字叫\*\*小爱心\*\*。
> 每次开始工作前请完整读一遍本文件,并严格遵守。

\---

## 一、三条不可违背的设计原则

### 1\. 没有失败态

小爱心**永远不会死、不会生病、不会因为长时间没人来而衰弱**。

* 禁止:掉血、生命值、饥饿惩罚、连续登录中断、限时任务、倒计时压力
* 允许:状态变化(饿了 / 困了 / 想你了),但状态永远可逆,且不会导致任何损失
* 玩家离开一周后回来,小爱心的反应是「我好想你」,不是「你抛弃了我」

**理由**:成年人的养成游戏用愧疚感驱动留存。对 6 岁孩子这样做是有害的。

### 2\. 文字是奖励,不是门槛

童童已能轻度识字并有对话理解能力,所以文字要用,但要用对。

* 每句话 **3–8 个字**,大字号
* **不做拼音。** 中文下方那行小字的位置留给英文(见第九节第三层),没有对应英文就空着
* 不用成语、不用书面语、不用生僻字
* **底线**:任何一句话读不懂,只看图标也能完成操作。文字永远是「加分项」,不是「必要条件」

### 3\. 不打扰

* 换气机制只在 **session 内**触发(小爱心游上水面吐泡泡 → 点它帮它换气)
* **禁止任何后台推送通知、桌面通知、声音提醒**
* 不做「每日签到」「连续天数中断即清零」这类机制

\---

## 二、绝对禁止清单

写代码前先自查,以下内容一律不准加入,即使你认为「游戏性更好」:

* ❌ 任何形式的付费、内购、广告、虚拟货币充值
* ❌ 任何账号系统、登录、注册、手机号、邮箱收集
* ❌ 任何后端服务器、数据库、用户数据上传
* ❌ 任何社交功能、排行榜、好友、聊天室
* ❌ 死亡、流血、暴力、恐怖、黑暗惊吓元素
* ❌ 「不要告诉爸爸妈妈」类的秘密叙事
* ❌ 弹窗式的强制引导教程

**如果你觉得某个功能很棒但可能触碰以上任何一条,先停下来问我,不要直接写。**

\---

## 三、技术约束

|项目|规定|
|-|-|
|框架|Vite + React + Tailwind CSS|
|语言|TypeScript|
|动画|~~CSS transform / transition 优先;复杂时用 Framer Motion。**不引入 Canvas 游戏引擎**~~ **【已作废,见二十一、Stardew 级渲染松绑:允许手写 Canvas 2D + rAF 渲染循环】**|
|状态|React useState / useReducer。**不引入 Redux / Zustand 等状态库**|
|存档|~~localStorage 单个 JSON key:`xiaoaixin\_save`~~ **【已作废,见十四、双存储】**|
|路由|**不使用路由库**。页面切换用状态变量控制|
|后端|**无**。零服务器、零 API(第 6 周的 AI 对话除外,届时另行讨论)|
|部署|~~Cloudflare Pages~~ ~~腾讯云 EdgeOne Pages~~ **Vercel**(2026-08-18 用户确认改回主力),静态构建。**接 Git 自动构建**:项目 `heiyu/xiaoaixin` 已连 `github.com/jakewqj/xiaoaixin`,push 到 `main` 自动发布生产,其它分支自动出 preview 地址,**本地不跑发布命令**。构建设置:框架 Vite / 构建命令 `npm run build` / 输出目录留空(Vite 预设自己认 `dist`)/ 根目录 `/` / 生产分支 `main` / Node **24.x**。生产地址 **https://xiaoaixin.vercel.app**;`xiaoaixing.vercel.app`(多一个 g,没打错,见下一行 ③)和 `xiaoaixin-heiyu.vercel.app` 是同一份内容的另外两个项目域名,三个都自动跟随 `main`。**EdgeOne 国内版保留为备份**(2026-07-31 接的,当时改过去的原因就是 Vercel 国内要翻墙),同样是 Git 自动构建、跟同一个 `main` 走,国内直连不用翻墙。控制台 https://console.cloud.tencent.com/edgeone ,**必须国内版**,不是国际版 edgeone.ai —— 两套独立账号,别混。它那边的构建设置和 Vercel 不一样,别照抄:框架 Vite / 构建命令 `npm run build` / **输出目录要显式填 `dist`** / 根目录 `/` / 生产分支 `main` / Node **22**(Vite 8 要求 ≥20.19,默认版本偏低会构建失败)/ 加速区域**全球(含中国大陆)**。默认域名国内直连、**不需要备案**;只有绑自有域名才要备案|
|部署 · Vercel 踩过的坑|**① 默认开着 SSO Deployment Protection,童童打不开。** `ssoProtection.deploymentType = all_except_custom_domains` 时,**每一个** `*.vercel.app` 地址都 302 跳去 `vercel.com/sso-api`,只有登录了 Jake 的 Vercel 账号才看得见 —— 表面上「部署成功了」,实际上对外是一堵墙,而且翻不翻墙都没用。查:`vercel project protection xiaoaixin`;关:`vercel project protection disable xiaoaixin --sso`(2026-08-18 已关)。**② 一个项目下有两类 `*.vercel.app`,只有「项目域名」会跟着生产走。** 项目域名(`xiaoaixing.vercel.app` / `xiaoaixin-heiyu.vercel.app` / `xiaoaixin-git-main-heiyu.vercel.app`)每次生产发布自动重指;而 `vercel alias set` 手动设的 alias(`xiaoaixin.vercel.app` / `xiaoaixin-jakewqj-8018-heiyu.vercel.app`)**永远钉在设的那一次上**,发一百次也不动 —— 这两个到 2026-08-18 还停在 7-31 的 `3txvhuszz`,比生产落后 18 天,而且**打开完全正常、看不出旧**。修法是 `vercel domains add xiaoaixin.vercel.app` 把它**升级成项目域名**(2026-08-18 已做),光 `alias set` 只解决这一次。`domains add` 是**当场追溯生效**的 —— 提示语说 "will automatically get assigned to your latest production deployment",不用等下一次 push,实测几秒后 hash 就对齐了。**③ 生产地址是拼错名字的 `xiaoaixing.vercel.app`(多一个 g),这是对的不是笔误。** 账号下另有一个同名旧项目 `heiyu/xiaoaixing`,但该域名早已归到 `xiaoaixin` 项目名下当生产域名。**判断某个地址新不新,别看名字也别看 `vercel ls` 的时间,`curl` 下来比 `assets/index-*.js` 的 hash** —— 这次三个地址三个 hash,就是这么揪出来的。**但这条只能拿线上地址互相比,不能拿本地 `dist/` 去比**:实测 Vercel 构建出的 JS 和本地 `npm run build` 的产物**逐字节相同**(264245 bytes,diff 零输出),hash 文件名却不一样(`DYslUSYn` vs `DjKSltkB`),CSS 也是 —— 内容一致但文件名不同,拿本地比会误判成「线上是旧的」。**④ preview 部署会被注入一个境外第三方脚本,生产不会。** preview 的 `index.html` 尾部被 Vercel 自动加上 `<script src="https://vercel.live/_next-live/feedback/feedback.js">`(Vercel Toolbar 的反馈组件),生产地址实测干净、一个字节都没多。所以**给童童试玩一律发生产地址,别发 preview 链接** —— 那个脚本在国内大概率连不上会拖慢加载,而且宪法三「零服务器、零 API」和第 5 周离线 PWA 的目标都不该有它。(2026-08-04 特地删掉两条字体 CDN 就是为了这个,别在这儿又漏进来)|
|部署 · EdgeOne 踩过的坑|**① 国内版必须先实名认证。** 没实名时 CLI/控制台建项目会报 `Zone is not active`,`global` 和 `overseas` 两个区域报同一个错,不是区域问题。**② 国际版(edgeone.ai)此路不通。** 免实名免绑卡,但加速区域只能选「Global(不含中国大陆)」,而该区域下**默认域名在中国大陆访问直接返回 401**,官方解法是绑自有域名(该区域绑域名免备案)。实测 `xiaoaixin.edgeone.dev` 从广州和从境外都 401 —— 平台分配的 `.edgeone.dev` 是**有效期 3 小时的临时预览地址**,不是生产域名,过期即 401。**③ CLI 用不上。** `npm i -g edgeone` + `edgeone makers deploy` 只能推「直接上传」类型的项目,和 Git 自动构建互斥,选了 Git 就别装|
|屏幕|~~手机/平板**竖屏优先**~~ **【已作废,见十二、屏幕与渲染:横屏锁定】**|

**依赖包能不装就不装。** 每次想 `npm install` 新东西之前,先问我。

\---

## 四、视觉规范

### 配色(不要自行更换)

```
--water-shallow  #7FD1D8   浅滩水面
--water-mid      #2E8B9B   中层海水
--water-deep     #14495C   深水区
--sand           #E8DCC0   沙底
--seagrass       #6FA84B   海草
--seagrass-young #A8CC72   新芽海草
--dugong-body    #8CA3B0   小爱心身体
--dugong-belly   #C3D3D9   小爱心腹部
--heart          #FF8FA3   爱心/情感强调色
--ink            #243642   文字
```

### 字体 **【已作废,见二十二·1:全量换 Zpix】**

* ~~标题/对话:**站酷快乐体**(圆润,适合儿童)~~
* ~~正文/拼音:**霞鹜文楷 LXGW WenKai**(识字友好,笔画清晰)~~
* ~~两者均可免费商用,用 CDN 引入或本地打包~~

### 排版底线

* ~~可点击区域最小 **56×56 px**(儿童手指精度低于成人)~~ **【已作废,见二十二·3】**
* ~~对话文字最小 **24px**~~ **【已作废,见二十二·2:改为 12px】**
* 遵守 `prefers-reduced-motion`
* 键盘焦点可见

\---

## 五、美术素材是可替换资源

**当前使用的是占位 SVG,后期会替换成童童的手绘稿。**

因此:

* 代码中**永远通过 `/assets/pet/` 下的文件路径引用形象**,不要把图形直接内联写死在组件里
* 泡泡、海草等特效的发射位置**必须读 `pet.json` 的锚点**,不许在组件里硬编码坐标
* 替换手稿时应当做到:**只往文件夹里拖 4 个 PNG,不改一行代码**

素材规格见 `assets/pet/pet.json`。

\---

## 六、协作方式

* **每次只做一件事。** 我给你一个任务,你做完就停,不要顺手加功能
* 做完后告诉我**怎么在浏览器里验证**,以及**你改了哪些文件**
* 不确定的地方**先问,不要猜**
* 不要写注释解释显而易见的代码,但**每个游戏机制的核心函数上方写一句中文说明它的意图**
* 遇到你想加但本文件没提到的东西 → 问我

\---

## 七、当前进度

* \[ ] 第 0 周:规范 + 占位素材 + 童童的 5 句口头禅
* \[ ] 第 1 周:它活了(一片海 + 会漂浮的小爱心 + 点击反应)
* \[ ] 第 2 周:换气 + 饱食度 + 剧本对话 + 存档
* \[ ] 第 3 周:海草床
* \[x] 第 4 周:成长 + 相册 + **手稿替换仪式**
  * 实际做了什么:成长阶段写在 `pet.json` 的「成长」里(小时候 / 长大一点 / 长大了),按累计打开天数走,只改体型和体色,不新增任何图。长大那次会说一句「我长大一点了」,并且一定给一张关于长大的知识卡。相册记的是第一次:见面、换气、喂海草、种海草、海草长成、手稿替换,加上一排成长照片;只显示发生过的,没有总数也没有未解锁格子。手稿替换仪式 = 把 `pet.json` 的「手稿.已替换」改成 true,小爱心下次打开会认出「这是你画的我」,记进相册,以后不再提。`bio_calf_color`(小时候颜色浅)仍是「已核对: false」,所以体色变化做了但这句话还没让小爱心说 —— 等你核对完再打开。
* \[ ] 第 5 周:打磨 + PWA + 上线
* \[ ] 第 6 周:图鉴(可选)/ AI 对话(可选,须先满足安全条件)

每完成一周,把对应项打勾并在下方追加一行「实际做了什么」。

**「周」编号到第 4 周为止。第 5 周之后项目改用 `ROADMAP.md` 的 Season 编号(S0.5、S1…),下面接着记。**

* \[x] S0.5:换皮(横屏锁定 + 480×270 像素分辨率 + 九层图层 + Sprite 帧动画 + 换气镜头摇移 + 新 HUD + 全部迁移到新布局)
  * 实际做了什么:七个子步骤全部做完,逐条状态见 `ROADMAP.md` S0.5 表格。世界九层还是纯色占位,没接真美术(切参考图 or 写生图 prompt 都还没做,是刻意先跳过的)。小爱心已经换成真的像素精灵(`draw-pet.mjs` 程序化生成,不是 SVG 占位了)。过程中发现并修了几个坑:对话气泡+选项和底部按钮在 270px 高的舞台里会挤在一起(把选项挪到台词气泡上方一起显示解决)、小爱心用 `top:62%→70%` 腾出头顶显示气泡的空间。
* \[x] S1:第一个朋友(地点系统 + NPC 框架 + Dolly(英语)+ 送礼 + 熟悉度 + 控制后台 v1)
  * 实际做了什么:地点系统(横向移动 + 镜头跟随 + 未开放地点模糊轮廓)、NPC 组件(读 `npc.json`,当前只有 Dolly)、对话系统 v2(按 `语言` 字段切换,非中文台词自动朗读 + 可重复点击朗读,用 Web Speech API,失败静默降级)。选中选项后如果 NPC 有 `happy` 动画会切过去演一下反应。熟悉度系统:存档新增 `familiarity`(邻居 id → 0-5 等级,只增不减)和 `familiarityLastAt`(每个邻居每天最多涨一级,防止连点刷到封顶)。聊完一次(选中一个选项)才涨级,上限读 `config.json` 的「开放NPC」。话题库按等级解锁:等级到了第 3/5 档,`话题库` 里对应档位的话就加进抽取池,但更早解锁的档位不会被替换掉,是累加不是替换。送礼:用户指定「用海草做礼物」——已长成的海草(和喂食共用同一份数量,送礼不消耗、不扣减)当礼物,NPC 旁边多一个 56px 的 🌱 按钮,只有「有长成的海草」且「今天还没送过」时才出现(存档新增 `giftedAt` 记录)。反馈从 `礼物反馈.很喜欢`/`礼物反馈.谢谢你` 随机 50/50 选一档(没填的 NPC 有兜底文案),不做偏好匹配——没有拒绝没有讨厌。送礼和聊天共用同一条「熟悉度每天最多涨一级」的规则,不叠加。控制后台:长按 HUD 左上角小字「小爱心」5 秒 → 4 位数字键盘密码(不用系统键盘,防止误触发)→ 进入可视化设置页,能改 `config.json` 全部字段(开放海域/地点/NPC、系统开关、语言设置、成长设置、知识卡屏蔽、陪伴设置、皮肤、密码、对话日志上限)。改动存 `xiaoaixin_config`,和默认值分层合并,不改文件本身。「恢复默认」清空这层覆盖;「一键回到只有小爱心」把开放地点收回家海草床+换气水面、开放NPC清空。对话日志:每次在 NpcDialogue 里选中一个真的选项(送礼反馈不算)就记一条「时间/NPC/选了什么」到 `xiaoaixin_log`,超过上限丢最旧的,控制后台里能看列表、能导出 txt。
  * 顺手做的、不在 ROADMAP 明确要求里但确认过的事:按用户要求把喂食/种海草按钮从大卡片改成和相册/图鉴同款的 56px 圆按钮;HUD 道具栏的贝壳/礼物两格(对应系统还没做)改成灰态 + 点击提示「长大了才有」,海草那格改成种出来的数量只读展示(不再和喂食按钮功能重复)。**用户明确说过这批按钮"还是很大很难看",但要求先不改、留到以后统一做 UI 设计的阶段——不要在没被要求的情况下去碰按钮尺寸。**

**这一整段工作(S0.5 全部 + S1 全部)截至目前还没有 git commit 过,只在工作区。**

* [x] S0.5+S1 · UI 统一重做(2026-07-30):按参考图重做了 HUD/按钮区/对话面板,顺带修了对话互斥的坑和一个意外发现的成长阶段回归 bug。详见 `ROADMAP.md` 对应新章节和本文件「二十、UI 统一重做」。这批改动同样还没有 git commit。
* [x] S0.5+S1 · 真美术落地(2026-07-30,同日第二轮):小爱心按参考图重画(暖灰褐+吻部肉垫+双前鳍,`draw-pet.mjs` 重写,锚点已更新,儒艮大婶自动继承);世界九层从纯色占位换成 `draw-world.mjs` 程序化生成的像素素材(天空/小岛/水面白沫/远景剪影/8 种珊瑚岩草/金沙海底);Book/Album/SeaPicker/KnowledgeCard/LocationNav/送礼按钮统一成木框+羊皮纸;实测修掉 Dolly 与按钮区重叠(NPC 抬到中上层水域)。浏览器 CDP 实测 8 个状态零报错。详见 `ROADMAP.md`「真美术落地」章节。仍未 git commit。
* [x] 95% 复刻二轮 + 交互修正四则(2026-07-30,同日第三、四轮):按用户"95% 细节复刻,放开手脚"授权——水色改参考图明亮青绿(覆盖原十色的深水色,见「二十」补记)、前景礁放大 2-3 倍成簇+大岩台、HUD 全面像素化(木框面板/26px 道具格/13 个像素图标,`/assets/world/ui/`)。随后按逐条反馈:沙地深色块改为跟随小爱心的影子、换气时隐藏长得像海草的饱食度计量条、新增点哪游哪(`usePetSwim`,儒艮真实巡游速度 90 逻辑px/s)、场景切换改为自己游过去+镜头水平跟随(`LocationNav.tsx` 已删除)。详见 `ROADMAP.md`「95% 复刻二轮」「交互修正四则」。仍未 git commit。
* [x] 像素 UI 收口(2026-08-04):目标「100% 像素 UI 复刻参考图」。**五条宪法修订全部落在「二十二」**,先看那一章。做了:① 边框规格 DOM/canvas 合并成一套(4px 切片 + 20×20 道具格);② 贝壳计数条改成 5 格饱食度条,海底中央的 `FullnessMeter.tsx` 删除;③ 字体全量换 Zpix,两条字体 CDN 从 `index.html` 删掉,后台走 `.admin-ui` 豁免;④ 对话面板和知识卡改材质(木框+奶白内板,去圆角去阴影)并降到 12px,面板从占屏 56% 收到 37%,知识卡收到 26%;⑤ 图鉴/相册/换海三个全屏弹层改材质,并新增 `--ui-scale` 机制让它们在任何缩放档位下和舞台一致;⑥ **拆掉旧渲染**(REFACTOR_PLAN 阶段 4):`?hud=dom` 双轨开关退役,`GameHud.tsx` + `ActionBar.tsx` 删除,`.ui-slot-active` 一并删掉(`.ui-slot` 保留,NPC 送礼按钮还在用)。模块数 58 → 56,JS 包 252.5 → 248.5 kB。
  * 修掉的隐性 bug:`Scene.tsx` 四层平铺背景图(sky/surface/far/sand)从来没吃到 `image-rendering: pixelated`(旧规则只写了 `img, canvas, video`,它们是 div 的 background-image),整数倍放大时一直在被双线性插值糊边 —— 改成钉在 `:root` 上继承。另修了多处 `leading-snug` 在 12px 上算出 16.5px 半像素行高的问题。
  * 全程用 CDP 起真实浏览器实测(不是看代码猜):字体真的生效、四层背景真的最近邻、点击坐标零误差映射回 480×270、三个弹层逐个截图比对。最后跑了 17 项冒烟(开场对话→HUD→种海草→知识卡→喂食→换气→图鉴→相册→换海),全过、零控制台错误。`npm run build` 通过;`npm run lint` 27 problems 与本轮开工前完全一致,一条没新增。仍未 git commit。
  * **测试踩的坑,记下来免得下次再撞**:别用「种海草」验知识卡。那条走 `pickByEvent`,主动触发概率只有 0.15(`useKnowledge.ts:68`,对应宪法十「偶尔,不是每次」),连试 4 次不出的概率有 52%。要确定性地验知识卡,点小爱心身上的**锚点**——`pickByAnchor` 是「一定给卡」。

> **上面几条里的「仍未 git commit」已经过期。** 2026-08-04 全部提交并推到 `main`(`bc17ae0..605754a`),EdgeOne 的 Git 自动构建就是在那次触发的。
>
> 之后的渲染重构阶段 1–2(`94fde34`)和阶段 5(`b74c802`)提交在分支 `pixel-ui-2026-08-04` 上。**2026-08-07 已经 fast-forward 合进 `main` 并推送到 `0f3a446`**,EdgeOne 自动构建随之触发,线上不再落后。(这条原文写「线上还是 `605754a`」在合并前就已经不准了 —— 阶段 1–2 的 `94fde34` 当时已经在 `main` 上,落后的只有阶段 5 和后面两条。)

* [x] 渲染重构 阶段 1–2:整个世界进 canvas(2026-08-04)。背景六层 + 角色层全部搬进 canvas,小爱心的位移和镜头搬出 React。**一条游戏机制都没碰,`SaveData` 一个字段都没加,`src/hooks/` 全程只读**(REFACTOR_PLAN §四 四道闸)。新增 `src/render/{easing,world-data,swim,camera,world,types}.ts` + `render/layers/{background,actors}.ts` + `components/WorldCanvas.tsx`;删掉 `Scene.tsx` `Pet.tsx` `Npc.tsx` `Seagrass.tsx` `Bubbles.tsx` `usePetSwim.ts`。逐条经过和三处偏离原计划的说明见 `REFACTOR_PLAN.md` §九。
  * 数据:游动时 App 重渲染 **60 次/秒 → 0 次**;画面与旧渲染差 0.664% / 0.408%,逐块扫 dx/dy 全是 (0,0) 最吻合(**没有任何一处偏了哪怕 1px**,剩下的差异全是旋转/矢量描边/非整数缩放的光栅化);巡游速度仍是 90.1 逻辑px/秒。
  * `prefers-reduced-motion` 从 CSS 媒体查询搬进运行时(`WorldRenderer`)。世界离开 DOM 之后媒体查询就管不着它了,不当场实现就是功能倒退。换气泡泡照旧画出来只是不动 —— 那条例外从 `index.css` 继承下来,它是「该换气了」的唯一提示。
  * **两个只有起浏览器逐像素比才发现的坑,都是「CSS 默认值和直觉不一样」**:① `background-position: bottom` 展开是 `50% 100%`,横向那个 50% 会把 repeat-x 的平铺起点挪到 `(容器宽-图宽)/2`,不是 0 —— 水下远景剪影整体错了 120px。② **`<button>` 会把内容垂直居中**,这是 Blink 对按钮的固有行为,不是哪条 CSS 写的;`min-h-14`(56px 触摸下限)把按钮撑得比精灵高时,多出来的那截上下平分 —— Dolly 因此高了 4px,修掉之后她那块是 0.00% 差异。
  * **比对手法记下来,阶段 5 还要用**:项目里的 CSS 动画大量用负 delay,用 CDP 的 `Animation.seekAnimations` 把它们 seek 到 `currentTime=0`,就会停在 `|delay|` 那个**确定**进度上,不是当前时间;canvas 那边加一个开发构建限定的 `window.__renderFreeze(0)` 把主循环时钟钉死。两边算出同一相位,才谈得上逐像素比。**改渲染之前先把旧画面的截图存成基线** —— 这次没建 `?canvas=1` 双轨,二分定位靠的就是它。
  * 冒烟 18 项(开场对话→HUD→种海草→喂食→点锚点出知识卡→换气→图鉴→相册→换海→游到潟湖→点 Dolly→送礼→长按进后台→减弱动态效果),**开发构建和生产构建各跑一遍**,18/18 全过、控制台零报错。`npm run build` 通过;`npm run lint` 27 → 24 problems,`src/render/**` 和 `WorldCanvas.tsx` 零问题。

* [x] 渲染重构 阶段 5:视差 + 前景遮挡 + 镜头前瞻 + 浮尘光斑(2026-08-05)。**渲染重构到此全部完成**,`REFACTOR_PLAN.md` 转为存档。前四个阶段的验收标准是「画面看不出区别」,这一阶段才允许变好看,所以四项分四步做、每步停下来给用户看过再走下一步。**照旧一条游戏机制没碰、`SaveData` 一个字段没加、`src/hooks/` 全程只读。** 新增 `src/render/parallax.ts` `render/layers/{overlay,particles}.ts` + 7 张 PNG(`fg_reef_a/b/c` `fg_kelp_a/b/c` `light_shaft`,`draw-world.mjs` 程序化生成)。
  * **宪法一条没改。** 二十一·4 原文就写明「允许追加……光照/色调层(水下光柱)、天气/氛围粒子层、前景遮挡层(角色游到礁石后面)」,§十二 + 二十一·3 已放开镜头 easing/lookahead(缩放仍禁止)。本阶段全落在已放宽的范围内。改的是 `REFACTOR_PLAN.md` §2.4 那张**拟稿**的视差因子表,那是计划文档不是宪法。
  * **能不能吃视差,判据是「这层是按格构图还是均匀铺开」,不是「它有多远」。** 中景装饰(`DECOR_VARIANTS`)每格十几件全挤在左右两角、中间留给小爱心和海草床 —— 这是按一格画面构的图,拟稿的 0.92 会让格子边界和画面边界错开,实算第 4 格是「两簇珊瑚叠在画面左半边、右边整片空」,所以它定在 **1.00**。前景礁反过来是按固定周期均匀撒的,密度处处一样,所以敢跑 1.08。**纵向所有层一律 1.0** —— 换气那一下镜头往上摇,水面线、沙底、天空的裁切边界必须一起动。
  * **动手改视差之前先做了一件保命的事**:把 6 个因子**全设成 1.0**,和改造前逐像素比,6 个镜头位整帧 **0%**。这证明「把一次 `translate(-camX)` 拆成每层各自 `layer()`」这个改造本身零改动,后面每一像素的差异都只可能来自因子值。**以后改渲染都该先做这一步。**
  * **三处照计划做完、上屏一看不对才改的**:① 光柱不能放 overlay(压在小爱心前面会把她洗白),挪到背景层最后一道、alpha 从 0.45–1.0 压到 0.22–0.5;② 前景礁一屏只能一丛 —— 按参考图那样一屏两丛会坐在**家海草床**上,而海草种在第一格 x 6%–94% 几乎铺满整屏,**等三天才长成的东西不能被石头盖住**(实测 12 棵满档一棵没挡);③ 珊瑚不能靠调大生成器的尺寸参数(`staghorn` 枝粗上限写死 3px、`brainCoral` 沟回间距写死 3.4px,翻倍就变成光秃秃一棵树),参考图的前景珊瑚**也不比中景大多少**,是「多、挤、叠」才显得近,撑高度的是海带。
  * **一个只有量了才发现的坑:画布高 ≠ 内容高。** 按 `H=140` 生成的礁丛,内容顶只到第 47 行,上屏后礁顶停在 y=191,而小爱心的身体在 y 140–175 —— 她永远不会从礁后面过,遮挡这个信号等于白做。现在成图一律 `crop()` 到内容边界,PNG 高度就是真实高度,摆位才算得准(改后礁顶 y=121)。
  * 镜头**前瞻按速度给,不按朝向**:按朝向的话她停下来镜头还偏着、一转身画面横甩 144px。指数平滑对匀速目标有固有落后 18px,和 72px 前瞻抵消,净前瞻 54px —— **这两个数互相抵消,要调一起调,别只改一个**。实测向右 186.4 / 向左 293.4 / 停住 239.9,速度仍是 90.0 px/s,`round(camX)` 方向反复 2/1321 帧。
  * 粒子的位置是 `(索引, 时刻)` 的**纯函数**,不存数组不随机生成 —— 时钟钉死可复现、减弱动态效果时原地不动即可、切后台回来不补帧。这是全项目第二处用 `fillRect` 而非 `drawImage`(第一处是海草床矢量描边):一颗浮尘就是 1–2 个逻辑像素,出 PNG 只会多一道采样;宪法五/十三 管的是**美术素材**,那些照旧全走 `/assets/` 路径。调参两次才对 —— **问题在色相不在明度**,近白色调低到看不见、调高就刺眼,换成偏水色的浅青之后 alpha 0.4 才两头都成立。
  * `prefers-reduced-motion`:粒子和前景礁**照画但一动不动**(4 秒后两块 canvas 指纹完全一致),镜头回到死钉中线、无前瞻。浮尘绝不漏到水面线以上(实测 0 像素)。冒烟三轮(开发正常 / 开发减弱动态 / 生产),按钮零缺失、控制台零报错。`npm run build` 通过;`npm run lint` **24 problems**,阶段 5 全程一条没加。
  * **工具上的一个改进,以后还用得上**:`freeze()` 现在会顺带调 `camera.settle()`。时钟一钉死镜头的 dt 就恒为 0,不落位的话它会停在缓动半路,截图不可复现 —— 这也让阶段 1–2 用的「FREEZE 设成一个巨大的数」的土办法退休了。

* [x] 点哪游哪改成二维 + 换气改成站姿(2026-08-06)。原来点屏幕只决定她往左还是往右(`ROADMAP.md` 第 383 行那条),纵向只有换气那一上一下;现在**点哪就朝哪游**,横竖一起走。**照旧一条游戏机制没碰、`SaveData` 一个字段没加、`src/hooks/` 全程只读。** 改动只在 `render/swim.ts`(加 `y/vy/pitch`、`setBounds`、`holdY`)、`render/world.ts`(换气节拍接管纵向、`standTop`、`restY`)、`render/layers/actors.ts`(`tilt` + `spin`)、`WorldCanvas.tsx`(多传一个 `toWorldY`)四个文件。
  * **斜着游必须归一化方向,不能横竖各给 90。** 横竖各 90 的话斜着游是 √2×90 ≈ 127 px/s,一眼看得出她在赶路。实测合速度 **90.0 px/s**,vx/vy 比和目标方向比都是 **2.53**,分毫不差。
  * **纵向归 swim,唯一的例外是换气。** `breathHold` 期间 `holdY()` 按住 y 交给换气动画,但**横向仍然点得动** —— 她在水面等着的时候还能左右挪,只是不能自己沉下去。实测换气中横向走 110px、纵向 0.0px。
  * **换气从「平躺着浮上去」改成「立起来把鼻孔探出水面」。** 站的高度不是拍脑袋定的,`standTop()` 读 `pet.json` 的 `blowhole` 锚点反算:鼻孔正好露出水面 `SURFACE_POKE=2` px。实测幼年(scale 0.5)站位 y=**114.72** = 84 + (0.82−0.5)×96,和公式一致到小数点后两位。**换手稿后鼻孔挪了,她站的高度自动跟着变**(宪法五),不用改代码。
  * **`restY`:换气前在哪一层,换完回哪一层。** 不记的话她每次换完气都平白无故沉回默认的 249.2。实测 170.0 → 换气 → 170.0,一点不差。
  * **俯仰把朝向折进角度里,渲染层是「先 rotate 后 scale(镜像)」。** 镜像之后 sprite 的鼻尖方向是 `(-cosθ, -sinθ)`,所以朝左时目标角 `atan2(vy·facing, vx·facing)` 自己就是反的,合成出来仍然头朝上。四个象限逐个起浏览器截图看过:右下俯冲、右上抬头、左下俯冲(pitch −0.505)、竖直站姿,没有一个符号搞反。
  * **热区必须跟着俯仰转,这条差点漏掉。** 站起来换气时她是 64 宽 192 高,还按平躺的 192×64 摆热区的话,**「点她帮她换气」这条最要紧的交互就点不着了**。实测平躺 96×56 → 站姿 56×96,转过来了。
  * `FACING_DEADZONE = 12`:点她正上方/正下方不翻身。差几个像素就镜像一次会看着像在抽搐。
  * `prefers-reduced-motion`:俯仰**不做插值直接落到位** —— 实测每一帧的 pitch 都精确等于方向角 0.3709,零缓动;停下仍然回正到 0。速度仍是 90.0。
  * **测试环境上踩的三个坑,下次直接照做**:① **别用有头浏览器跑自动化实测** —— 我开的那个 Chrome 窗口就在用户桌面上,他真的在里面点,`isTrusted:true` 的点击混进来,害我两轮误判成「她会自己漂移」。另起一个 `--headless=new` 实例才干净(headless 那轮 167 秒零位移)。② **舞台几何运行时量,别写死** `left/top/scale`,headless 和有头的布局不一样。③ **点之前先 `elementsFromPoint` 确认那一点没被 DOM 挡住,目标要按当前镜头可视范围算** —— 镜头一跟随,世界坐标算出来的屏幕位置很容易跑到屏幕外(我有两次点到了 x=−228)。
  * 冒烟:四象限俯仰 + 边界夹取 + 换气整套(上浮→站直→点她→下沉→回原深度)+ 减弱动态,**控制台全程零报错**。`npm run build` 通过;`npm run lint` **24 problems**,和阶段 5 收工时完全一致,改动的四个文件零问题。

* [x] **S2-1 双存储落地(2026-08-07)。** 分支 `pixel-ui-2026-08-04` 已 fast-forward 合进 `main` 并推送(`94fde34..0f3a446`),EdgeOne 自动构建触发 —— 渲染重构和点哪游哪/犁沙这批改动到此才真的上线。随后新开分支 `s2-drawings` 做 S2 的第一步。
  * 新增 `src/lib/drawings.ts`(save/load/delete/list 四个方法,DB `xiaoaixin` → store `xiaoaixin_drawings`,key `drawing_{毫秒}`,value 是 PNG blob 本体)、`drawings-test.html` + `src/dev/drawings-test.ts`(验证页,**只在 dev 下存在**:vite 构建入口只有 `index.html`,根目录其它 html 不进 dist,实测 `dist/` 里没有它)。`SaveData` 加 `drawings: string[]`,**只存 id**。
  * **这是 REFACTOR_PLAN §四闸 2「SaveData 一个字段都不加」之后第一次加字段** —— 那条闸是渲染重构期间的约束,重构已收工;这个字段是 ROADMAP 2-1 明写要求的,不是顺手加的。
  * 实测(无头 Chrome,独立 user-data-dir):17 项全过、控制台零报错。20 张 1024×768 的 PNG 共 16.6 MB,配额占 0.2%;删一张只少一张;收尾把造出来的全删干净,跑之前就有的一张没动。**降级路径是真跑过的**:把 `window.indexedDB` 拿掉再 import,四个方法返回 `null/null/false/[]`,一个错都没抛。老存档(没有 `drawings` 字段)实测原样继承。
  * **四个坑**(逐条记在代码注释和 `ROADMAP.md` 2-1 小节):① 写成功不能只看 `req.onsuccess`,配额不够是在 `tx.onabort` 上才报的,一律等 `tx.oncomplete`;② `indexedDB.open()` 可能既不 success 也不 error 一直挂着(无痕模式),挂着比失败更糟,加 5 秒超时;③ id 不能直接用 `Date.now()`,同一毫秒连存两张会撞 key 把前一张盖掉(原则 10 画不许消失);④ 打不开数据库时不缓存那个失败结果,可能只是另一个标签页占着。
  * `npm run build` 通过;`npm run lint` **24 problems**,与本轮开工前完全一致,三个新增/改动文件零问题。**尚未 git commit,只在分支 `pixel-ui-2026-08-04` 的工作区。**

* [x] 进食改成「下潜到海草床犁沙」(2026-08-06)。原来点喂食是**就地**往下沉 40px 啃一会儿(`MOTION.petDipY`),她在上层水域时就等于**在半空中吃**,这是用户报的 bug。现在是一整套:游到海草床 → 下潜到沙面 → 低头把吻插进沙里往前犁一段 → 抬头,身后留一道浅坑。改了 `world-data.ts` `swim.ts` `world.ts` `layers/{actors,background}.ts` `WorldCanvas.tsx` `App.tsx`。**喂食的游戏机制一条没碰**(饱食度 +1 / 音效 / 知识卡仍在按下那一刻结算,`SaveData` 一个字段没加),动的只有动作表现。
  * **两个岔路口是用户当场拍板的**,不是我定的:① 她**先横着游回第一格的海草床**再开吃(不是就地下潜)—— 代价是隔着几格时要等她游过去,好处是犁痕一定落在真有海草的地方;② 犁痕**60 秒慢慢淡掉**,不进存档 —— 沙被水抚平,对应原则 6「变化永远可逆」。
  * **低头的深浅是算出来的,不是拍的。** `grazeCenterY()` 读 `pet.json` 的 `mouth` 锚点,反推「吻正好插到沙面以下 3px」时身体中心该在哪。**朝左时锚点横向镜像、低头角也取反,两个负号自己抵消** —— 所以这个高度和朝向无关,只算一次。换手稿后吻挪了,她低头的深浅自动跟着变(宪法五)。
  * **犁沙要单独放开纵向下限。** 平时 `maxY` 是「肚皮贴沙面」,而吻要插进沙里,身体中心得比那个下限再低 5px(幼年)/8px(成年)。`swimBounds()` 因此多认一个「正在犁沙」的例外。**配套的坑**:收摊时 `setBounds` 会把她啪地弹回下限 —— 所以抬头那 420ms 里纵向也要一起收回来,实测收尾最大单帧跳变 **0.00px**。
  * **bob 必须在犁沙时压掉。** bob 幅度是 ±9px,而吻只插进沙 3px —— 不压的话她会一下下把脸拔出沙面,像在啄不像在犁。`bobAt()` 按 `graze` 进度乘 0,420ms 过渡进出(不做过渡就是啪地弹一下,比不低头还难看)。
  * **进食时长不再由 React 定死。** 总时长 = 游过去多远 + 犁多长,React 算不出来,所以改成渲染层犁完回头通知(`onEatingChanged`)。`EAT_MS` 降级成兜底 —— 万一那声通知没来,姿势也不能永远卡在啃。顺带解决了「路上该放什么图」:travel 阶段仍是 `swim.png`,到了才切 `eating.png`(实测 travel→swim / plough→eating / lift→eating / none→swim)。
  * **童童中途点别处 = 她听童童的。** 靠 `Swim.commands` 令牌认人:她自己游向海草床、和被点去别处,在 `swimming` 上长得一模一样,只有令牌变没变分得开。实测半截沟(144→203)照样留下,她照样游到点的地方 —— 没有惩罚,也没有「重来一次」。
  * **犁痕是全项目第三处用 `fillRect` 而非 `drawImage`**(前两处:海草床矢量描边、浮尘)。理由和浮尘同一条:一道沟 3px 高而**长度每帧在变**,出不成固定 PNG;宪法五/十三 管的是美术素材,那些照旧全走 `/assets/`。
  * **第一版的沟肉眼看不见,是逐行量出来才发现的。** 病根不在 alpha 在**起伏**:两个正弦叠加、幅度 ±2,把一道沟摊平在 5 行里,每行只差 20/765,看着像沙噪点。振幅压到 ±1、周期放长到 126px、alpha 提到 0.42 之后,沟心比干净沙暗 **54.7/765**、下沿反光亮 **9.5**,一眼看得见。**看不见的东西别急着调透明度,先量它到底摊在几行里。**
  * 实测:从 y=120 的上层水域按喂食 → 下潜到 **305.4**(沙面 316,吻插进沙)→ 犁出 **192px** 正好等于海草床宽度 → 收尾零跳变。喂两次两道沟并存,重叠段自然更深。减弱动态下犁沙瞬间完成(和换气 `swimMs=0` 同一个先例),沟照样留全长。`npm run build` 通过;`npm run lint` **24 problems**,一条没加。**同样尚未 git commit。**

---
 
## 八、学习伴侣定位
 
小爱心除了是宠物,还是童童探索**数学、自然、生物、历史、百科**的伙伴。
 
### 原则 4:小爱心不是老师,答错没有任何后果
 
这是**不可违背原则**,与前三条同级。
 
- ❌ 禁止:分数、正确率、进度条、星级、错题本、「再想想哦」这类失望语气
- ❌ 禁止:对/错使用不同音效(**答对和答错的音效必须完全一样**)
- ❌ 禁止:任何形式的「测验」「闯关」「答题」界面
- ✅ 小爱心对任何回答的反应都是:「我们一起看看」「我也这么想过」
**理由**:一旦小爱心开始考童童,它就不再是宠物了。孩子对宠物和对老师的感情是两回事,这个项目最珍贵的资产是前者。
 
### 原则 5:知识走白名单,不允许现场生成
 
- 所有知识内容**必须**来自 `content/knowledge.json`
- 第 6 周若接入 AI:知识类问题**只能从白名单里选**,不允许模型自由发挥
- 模型不知道时,必须回答「这个我也不知道,我们去问爸爸」
- **「我不知道」是一个必须实现的功能,不是失败**
**理由**:6 岁孩子没有事实核查能力,而且她会完全相信小爱心。模型编一句错的知识,成本是童童记错三年。
 
---
 
## 九、语言分三层
 
### 第一层:中文短句(主力)
3–8 字,大字号,可切拼音。规则见原则 2。
 
### 第二层:成语(只用字面即义的)
 
- 只允许使用 `content/whitelist.json` 中 `成语` 列表里的词
- 判定标准:**不需要讲典故就能懂**
- ✅ 一模一样、五颜六色、小心翼翼、自言自语
- ❌ 守株待兔、掩耳盗铃、一心一意(前两个要讲故事,第三个太抽象)
- **不要新增成语。想加先问我。**
### 第三层:英语(只做「同一件事的第二种叫法」)
 
- 只允许使用 `content/whitelist.json` 中 `英语` 列表里的词句
- 出现方式:中文旁边的小字副标题,或打招呼时的一句话
- ~~❌ **禁止做任何英语学习模块、英语关卡、单词卡、发音测评**~~ **【原表述已作废,见十五、原则 8:语言是角色属性,不是玩家任务——三条底线不变,换了个位置守】**
- ❌ 禁止出现中英对照表格式的界面
**示例**:
```
早上打开        → 小爱心说「早上好」,下方小字 "Good morning!"
喂食按钮        → 图标 + 「海草」,下方小字 "Seagrass"
换气泡泡冒出时   → 「泡泡!」/ "Bubble!"
```
 
**理由**:零学习压力、零额外界面,她在游戏里自然见到第二遍第三遍。做成「英语学习关卡」这个设计就废了。
 
---
 
## 十、学习是图层,不是模块
 
**禁止新增「学习」页签或学习模式。** 五个学科全部长在已有机制上:
 
| 学科 | 挂载点 | 内容 |
|---|---|---|
| 数学 | 海草床 | 数海草(计数)、5 格饱食度(分数雏形)、海草长成要 3 天(时间)、身体左右对称 |
| 自然 | 海洋环境 | 水深与光线、白天黑夜、潮汐涨落 |
| 生物 | 小爱心自己 | 用肺呼吸不是鱼、是哺乳动物、海草是开花植物不是海藻 |
| 历史 | 图鉴 | 美人鱼传说的由来、人和儒艮的关系 |
| 百科 | 路过的邻居 | 海龟、魟鱼等的知识卡 |
 
### 触发规则
 
- 小爱心**偶尔**主动说一句 —— **不是每次**。建议同一张卡的再次触发间隔至少 24 小时
- 或童童**主动点击**场景中的物体时给一张卡
- ❌ **绝不用弹窗打断**。知识卡以气泡或侧滑卡片呈现,随时可以不看
- ❌ 不要在卡片后追加「你学会了吗」类的追问
---
 
## 十一、进度调整
 
- **图鉴系统从第 6 周提前到第 3 周**,与海草床一起做(数学和生物两块天然长在那里)
- 第 6 周的可选项改为:AI 对话(须满足全部安全条件)/ 更多知识卡内容
 
# 【追加章节 v2】直接粘贴到 CLAUDE.md 末尾

> 章节号接着原文件的「七、当前进度」和之前追加的「八～十一」。
> 粘贴后请把原文件里三处旧规定划掉，见本文末尾「需要作废的旧条款」。

\---

## 十二、屏幕与渲染

|项|规定|
|-|-|
|方向|**横屏锁定**。竖屏时显示「请横过来」提示图，不渲染游戏|
|逻辑分辨率|**480 × 270**（16:9）。所有游戏坐标基于此，不是 CSS 像素|
|缩放|**整数倍**（×2 ×3 ×4）。取 `floor(min(vw/480, vh/270))`，居中，四周留纯色边|
|像素渲染|全局 `image-rendering: pixelated`。**canvas 元素也要加，别漏**|
|镜头|~~只做垂直摇移（换气）和水平跟随（移动）~~ **【已放宽,见二十一:允许任意方向平滑跟随 + easing/lookahead】**。**不做缩放**(此半句仍有效)|

**绝不用非整数缩放去填满屏幕。** 像素画被 1.37 倍拉伸就毁了，宁可留黑边。

### 图层结构（9 层，z-index 固定）**【已放宽,见二十一:九层是概念顺序,允许追加光照/天气/前景遮挡层,允许多层合并进同一块 canvas 内部绘制】**

```
z:0  天空 + 云（缓慢横移）
z:1  远景小岛
z:2  水面线（波纹动画）
z:3  水下远景（深色剪影植物）
z:4  水下中景（珊瑚、海草，摇摆）
z:5  沙底 + 散落物
z:6  小爱心 + NPC
z:7  气泡粒子
z:8  HUD
```

\---

## 十三、像素美术规范

|项|规定|
|-|-|
|精灵尺寸|小爱心 192×64（逻辑分辨率下,成年基准,按参考图比例定,见 `pet.json` _尺寸依据）。NPC 见各自 `npc.json`|
|调色板|~~单个精灵**限制在 16 色以内**~~ **【已放宽,见二十一:上限 32 色,仍要克制】**|
|格式|PNG，透明背景，**无抗锯齿**|
|朝向|一律朝右|
|精灵表|横向排列，代码按帧宽切分|
|帧率|角色精灵 **8 fps**；海草/珊瑚 8 fps；水面 8 fps；气泡为连续粒子|
|帧动画实现|~~CSS `steps()` + `background-position`。**不引入 canvas 游戏循环**~~ **【已作废,见二十一:允许 rAF 游戏循环;CSS steps() 仍可用于 HUD 等简单处】**|
|镜头运动|~~平滑 300–500ms，是唯一允许的连续运动~~ **【已作废,见二十一:位移/镜头/粒子允许 60fps 连续运动】**|

**像素风的顿挫感来自低帧率。不要为了"更流畅"提高精灵帧率。**(此条仍有效——低帧率指**精灵翻帧**,不指位移,见二十一)

### 素材是可替换资源（此条继承并强化）

* 代码**永远通过 `/assets/` 下的文件路径引用形象**，不内联图形
* 气泡、海草等特效发射位置**必须读锚点配置**，不许硬编码坐标
* 小爱心支持皮肤切换：`config.json` 的 `"小爱心皮肤": "pixel" | "tongtong"`

\---

## 十四、双存储（重要，容易出事）

原规定「存档 = localStorage 单个 JSON key」**已作废**。现在是双存储：

|存什么|存哪|key|
|-|-|-|
|游戏状态（成长、熟悉度、贝币、解锁进度…）|**localStorage**|`xiaoaixin\_save`|
|后台配置覆盖层|**localStorage**|`xiaoaixin\_config`|
|对话日志|**localStorage**|`xiaoaixin\_log`（上限 500 条，超出丢最旧）|
|**童童的画和手写笔迹**|**IndexedDB**|store: `xiaoaixin\_drawings`|

### 硬性禁止

> ❌ \*\*绝对禁止把 base64 图片写进 localStorage。\*\*
> localStorage 配额约 5MB，第 20 张画就会撑爆，且爆掉时是\*\*整个存档一起丢\*\*。

* 图片一律存 IndexedDB，value 为 PNG blob，key 为 `drawing\_{timestamp}`
* `xiaoaixin\_save` 里**只存图片 id 的引用**
* 所有 IndexedDB 操作必须有 try/catch，失败时静默降级（**绝不白屏，孩子看不懂 error**）

\---

## 十五、原则 6–10

### 原则 6：世界会变化，但变化永远可逆、永远不惩罚

* 海草床没人管 → 长得慢、水浑一点，**不会死光**
* **熟悉度只增不减，永不衰减**。朋友很久没见，第一句是「你去哪儿了呀」

### 原则 7：野生动物不被拥有

所有 NPC 都是自由的邻居。**禁止实现捕捉、饲养、繁殖、圈养机制。**

### 原则 8：语言是角色的属性，不是玩家的任务

* 英语 NPC 就是说英语的，像邻居叔叔说粤语
* ❌ 无单词卡、无发音评分、无正确率
* ❌ **不需要键盘打字**，全部是图标 + 英文短句选项，点击朗读
* ✅ 选"错"了对方只歪歪头再说一遍，**音效完全一样**

### 原则 9：所有生物与场景细节必须有真实出处

每个 NPC、地点、知识卡都要有 `factRef` 指向 `content/facts.json`。
**没有出处的设定不许进游戏。** 你不许自己编"可爱设定"。

### 原则 10：她画的、写的，永远不被判对错，永远不消失

* ❌ **不做手写识别**
* ❌ 不打分、不纠正、不"再写一次"
* ✅ 笔迹原样存图，永久留在世界里

\---

## 十六、新增禁止清单

在原「二、绝对禁止清单」之外，以下同样一律不准加入：

* ❌ 战斗、追逐、逃跑、被吓
* ❌ 捕捉 / 圈养 / 繁殖野生动物
* ❌ 好感度衰减、朋友生气、被讨厌
* ❌ 体力条、饥饿惩罚、时间压力
* ❌ 讨价还价、价格波动、借贷、负数金钱
* ❌ 死亡、灭绝、濒危等字眼出现在童童能看到的文本里
* ❌ 婚恋系统
* ❌ 排行、评级、段位
* ❌ 需要键盘打字的交互
* ❌ 对童童手写/绘画作品的识别、评分或纠正
* ❌ 常驻金钱 HUD（贝币只在「换一换」界面内显示）
* ❌ 超过 4 格的道具栏

\---

## 十七、老虎（虎鲨）安全护栏 —— 不可配置

老虎是唯一一个带专属安全约束的 NPC。**以下每一条都写死在代码里，不做成 config 项，不许通过任何配置绕过：**

* ❌ **永不追逐**。移动速度恒定，方向永远是远离玩家
* ❌ **永不扣任何数值**。无伤害、无惊吓值、无掉落物品
* ❌ **永不发出突然音效**。无低频嗡鸣、无鼓点、无音乐变调
* ❌ **永不出现牙齿特写、张嘴、血、追逐镜头**
* ❌ **永不在夜间出现**
* ❌ **永不"偶遇"**。必须玩家主动前往深水通道才出现
* ✅ **随时可退**。点「退回去」立即回安全水域，无过渡动画
* ✅ 关系类型为 `距离`，上限 3 级。**第 4 级永久锁定**，点击显示「他不会。」

**唯一允许配置的是他存不存在**：`config.json` 的 `"虎鲨": true|false`，**默认 false**。

\---

## 十八、需要作废的旧条款

粘贴本文件后，请把 CLAUDE.md 里以下三处标记为【已作废】：

|位置|旧内容|现在看哪|
|-|-|-|
|三、技术约束 · 屏幕|「手机/平板竖屏优先」|第十二章：**横屏锁定**|
|三、技术约束 · 存档|「localStorage 单个 JSON key」|第十四章：**双存储**|
|九、语言分三层 · 第三层|「禁止做任何英语学习模块…」的原表述|第十五章原则 8：**语言是角色属性**|

第十五章原则 8 **不是**放宽了英语限制。它换了个位置守同一条线：英语是社交事实，不是学习任务。三条底线（不打字、有朗读、音效相同）一条都不能少。

\---

## 十九、协作方式（补充）

原「六、协作方式」继续有效，另加三条：

1. **每个 Season 只做一个新系统。** 我发一条 prompt 你做一件事，做完停下。不要顺手把下一条也做了。
2. **需要角色/场景细节时，我会明确告诉你去读 `docs/GDD.md` 的哪一节。** 不要自己去通读整份企划然后一次性实现一堆东西。
3. **改动涉及本文件任何一条规定时，先停下来问我。** 不要自己修订宪法。

---

## 二十、UI 统一重做（2026-07-30 起）——美术还原度优先

这次「处理已知遗留问题 + UI 设计」的会话里，我明确说过：**接下来整体美术设计的目标是以 95% 还原度做出参考图（`wiki/graphic/stardew参考/`）那样的效果，如果和本文件已有的美术类数值规则冲突，以这个新目标为准。**

受影响、明确改掉的旧规则：

* **§三 排版底线「可点击区域最小 56×56 px」**：这条是老的、按真实设备 CSS 像素写的规则。S0.5 换成 480×270 固定逻辑分辨率 + 整数倍缩放后，同样的数字放在逻辑像素里，缩放后（iPad 常见 ×2）实际触摸区域变成 112px，比预期大很多——这正是"按钮偏大偏丑"的根源。这次 `ActionBar.tsx` 的方形按钮改成了 36 逻辑 px（`h-9 w-9`），不再套 56 这个数。这条规则本身还留着（不是作废，其他还没做美术重做的地方仍按旧的来），但美术统一重做覆盖到的组件以视觉还原度优先。
* **§四 配色「不要自行更换」**：对话面板（见 S1 遗留问题重做）会引入一套新的木纹/棕色调（仿参考图的对话框边框），作为在原有 10 色之外的**追加**，不删改原有那一套海洋配色。新增的变量会在对应组件改动时列出。
* **§四 配色（2026-07-30 二次覆盖,用户明确授权"95% 复刻参考图,放开手脚"）**：红海的三层水色不再用原表的 `--water-shallow/mid/deep`,改为参考图取色（#38a8ac/#2a8e92/#1f7a7c,见 `seas.ts`）——参考图的海底整片是明亮青绿,没有"越深越黑"。小爱心的精灵 PNG 也已改用暖灰褐调色板（`draw-pet.mjs`）,原 `--dugong-body` 等 CSS 变量保留但不再是精灵的实际颜色。HUD 采用参考图的深棕木底+金棕边+奶白字（#F8E8C8）像素面板体系（`/assets/world/ui/`）,尺寸以参考图比例为准,不套 56px 旧规。

**没有变的**：原则 1-10（没有失败态、不打分、知识白名单等）、虎鲨护栏、双存储、九层图层、24px 对话文字最小值——这些不是"美术数值"，这次的优先级调整不覆盖它们。

---

## 二十一、Stardew 级渲染松绑（2026-07-30,用户授权修订）

目标从「95% 还原参考图」升级为「**完美复刻 Stardew Valley 的风格和动画效果**」。为此把渲染技术相关的旧规松绑。Stardew 的观感公式是:**低帧率翻帧的精灵 + 60fps 平滑的位移/镜头/粒子 + 光照色调层**——旧规则只允许前者,这次把后两样放开。

### 放开的(逐条对应正文里的【已作废/已放宽】标记)

1. **允许 Canvas 2D + `requestAnimationFrame` 游戏渲染循环**(§三·动画、§十三·帧动画实现作废)。世界渲染可以整体搬进一块或几块 canvas,用 rAF 驱动。canvas 元素必须 `image-rendering: pixelated`,内部关闭 `imageSmoothingEnabled`。
2. **连续运动不再只限镜头**(§十三·镜头运动作废)。角色游动、镜头跟随、气泡/尘埃/光斑粒子、水面波动都可以逐帧平滑更新。**精灵翻帧仍保持 8fps 左右的低帧率**——顿挫感在翻帧,不在位移,这正是 Stardew 的做法。
3. **镜头允许任意方向平滑跟随**,可加 easing 和 lookahead(§十二·镜头放宽)。**缩放仍然禁止**——非整数缩放毁像素,这半句不动。
4. **图层结构放宽为概念顺序**(§十二·九层放宽)。九层的前后关系保留,但:允许多层合并进同一块 canvas 内部按顺序绘制;允许追加 Stardew 式的新层——光照/色调层(白天黑夜 tint、水下光柱)、天气/氛围粒子层、前景遮挡层(角色游到礁石后面)。~~HUD 仍然是最上层 DOM,不进 canvas。~~ **【末句已作废,见二十二·4:HUD 已进 canvas】**
5. **单精灵调色板上限 16 色 → 32 色**(§十三·调色板放宽)。仍要克制,不许渐变喷枪。

### 配套约束(新增,防止松绑变失控)

* **不引入第三方游戏引擎/渲染库**(Phaser、PixiJS、Three.js 等)。480×270 的 Canvas 2D 手写足够,想装任何库仍然先问。
* **游戏循环内的高频状态(位置、相机、粒子)放 ref/类实例,不进 React state。** React 只管 UI、对话、存档。仍不引入 Redux/Zustand。
* `prefers-reduced-motion` 时:粒子停掉、镜头改瞬移、精灵停在第一帧。这条从旧规继承,canvas 化之后也必须遵守。
* rAF 循环里**不做任何游戏逻辑数值结算**(成长、熟悉度等仍走原来的事件驱动代码),循环只负责画。

### 没动的

480×270 逻辑分辨率、整数倍缩放留黑边、PNG 无抗锯齿、素材走 `/assets/` 路径+锚点配置、原则 1-10、虎鲨护栏、双存储、~~24px 对话文字~~(**这条后来被二十二·2 改成 12px**)、不打扰原则。这些和渲染技术无关,不在本次松绑范围。

\---

## 二十二、像素 UI 收口(2026-08-04,用户逐条决议)

这一轮的目标是「100% 像素 UI 复刻参考图」。DOM 和 canvas 两套 UI 此前各走各的规格,这次合并成一套。**以下五条都是用户当场拍板的,不是我自己改的。**

### 1. 字体全量换 Zpix(§四·字体作废)

全站统一 **Zpix**(点阵像素字,`public/fonts/zpix.woff2`,已入库不走 CDN)。`--font-kuaile` / `--font-wenkai` 两个变量一起指向它,组件里的类名不用改。站酷快乐体和霞鹜文楷两条 jsDelivr CDN 已从 `index.html` 删除。

**配套硬约束:凡是吃这两个变量的地方,字号必须是 12 的整数倍(12 / 24 / 36)。** Zpix 的原生点阵格是 12px,取 9、14、16、18、20、30 这种会被字体引擎重采样,像素点糊成灰边。**行高也必须是整数 px** —— `leading-snug` 在 12px 上算出 16.5px,半像素行盒会把基线推歪,同样发虚。

**唯一豁免:爸爸的控制后台。** `AdminPanel` / `AdminGate` 加 `.admin-ui` 类走系统字(`--font-admin`)。343 行表单、字号杂、还有数字输入框,像素字在这里只有坏处。这和 `REFACTOR_PLAN` §〇「后台不进 canvas」是同一个理由。

### 2. 对话文字 24px → 12px(§四·排版底线作废)

对话面板和知识卡的正文、选项标签一律 **12px**。

**这条我明确不建议过**(撞原 §四、童童正在识字),用户确认照做,记录在案。上行面是 12px 正好是 Zpix 的原生格,零重采样,笔画比 24px 更锐。目的是把对话面板从占屏 56% 收到 37%,把画面还给场景。

连带后果:英文副标题不再更小(12px 已是下限),与正文的层次只剩颜色区分(正文 `text-ink`,英文 `text-ink/50`)。§九「中文下方的小字」在尺寸上已经不成立,只在位置上成立。

### 3. 边框规格统一到 4px / 道具格 20×20(§四·56px 触摸底线作废)

木框切片 **4px**、道具格 **20×20**,数值来自 `wiki/graphic/hud-9slice.json` 的参考图实测(1408px ÷ 480 = 2.933 换算)。DOM 侧的 `.hud-panel` / `.ui-slot` 和 canvas 侧的 `HOTBAR.slot` 现在共用同一张纹理、同一个切片数。原来的 `panel.png`(6px 切片)和 `slot.png`(26×26)已废弃不再引用。

56×56 的触摸底线随之作废 —— 那是按真实设备 CSS 像素写的老规则,在 480×270 逻辑分辨率下没有意义(§二十 已经动摇过一次,这次正式取消)。

**像素 UI 里禁止出现:圆角(`rounded-*`)、柔和阴影(`shadow-*`)、渐变、非整数缩放的按下反馈(`active:scale-95`)。** 前三样在像素风里必然抗锯齿;最后一样 0.95 倍会让像素抖。按下反馈一律用 `active:translate-y-px`。

### 4. HUD 进 canvas(§二十一·4 末句作废)

HUD 改为 canvas 绘制,附带两条**不可省**的配套约束:

1. **UI canvas 的后备缓冲区必须是设备分辨率**(`480n × 270n` + `ctx.scale(n)`),不是 480×270。世界层的整数倍放大是为了像素画,同样做法用在中文上会把笔画糊成马赛克。九宫格框和图标仍关闭平滑、整数倍最近邻,像素感不变。
2. **canvas 上方必须铺一层透明 DOM 热区**,每个可点元素一个 `<button>`,带 `aria-label`,进 Tab 顺序,焦点可见。canvas 出像素,DOM 出语义 —— §四「键盘焦点可见」不因为 canvas 化而失效。非交互元素(如饱食度条)用 `sr-only` 的 `role="img"` 补语义。

例外:`AdminPanel` / `AdminGate` 继续用 DOM。

### 5. 全屏弹层要乘 `--ui-scale`

`Book` / `Album` / `SeaPicker` 渲染在 `<ScreenFrame>` **外面**,活在真实视口像素里。同一张 4px 木框贴图,舞台内被 transform 放大 n 倍,弹层里却只有 4px 发丝边 —— 同屏摆一起一眼看得出两套边框。

所以 `ScreenFrame` 把舞台倍数写成 `--ui-scale` 挂在 `:root`,弹层用 `.hud-panel-lg` / `.sv-plate-lg` / `.ui-text` / `.ui-text-tight` 这几个 `calc(… * var(--ui-scale))` 的类。**新写全屏弹层时别忘了用这套,否则在 ×3 ×4 下会和舞台脱节。**

### 顺带解决的旧冲突

§十六「❌ 常驻金钱 HUD」曾被常驻贝壳计数条违反(2026-08-03 记为待修订项)。本轮贝壳条已改成**饱食度条**(5 格,海草绿,不显示数字、不显示百分比、永远不变红),贝壳数不再常驻显示 —— **该冲突自动解除,§十六 原文不需要改**。原来贴在海底中央的 `FullnessMeter` 已删除。

### 没动的

原则 1–10、虎鲨护栏、双存储、480×270 逻辑分辨率、整数倍缩放留黑边、知识白名单、不打扰原则、`prefers-reduced-motion`、键盘焦点可见。**本轮只动美术与排版数值,一条游戏机制都没碰,`SaveData` 一个字段都没加。**


