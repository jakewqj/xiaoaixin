const fs = require('fs'); const path = require('path'); const imageFiles = ['idle.png', 'happy.png', 'talk.png']; const outputInfo = []; for (const f of imageFiles) { const fullPath = path.join('D:\\xiaoaixing\\public\\assets\\npc\\dolly', f); try { const stats = fs.statSync(fullPath); outputInfo.push({file: f, sizeInBytes: stats.size}); } catch (e) { outputInfo.push({file: f, error: e.message}); } } console.log(JSON.stringify(outputInfo, null, 2));
