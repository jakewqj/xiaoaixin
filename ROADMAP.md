# 小爱心 · Claude Code 开发路线图 v1

> 配套文档：`docs/GDD.md`(企划,"为什么"的唯一权威)
> 本文件是**执行文档**：给 Claude Code 的分阶段任务、prompt 序列、数据结构、验收标准。
>
> **铁律不变**：每个 Season 结束必须是能给童童玩的完整体验。

---

## 0. 先看这里：参考素材带来的 3 个变更

你上传的像素画确定了美术方向，但它同时改了三件已经定好的事。这三件必须先决定，否则 S1 会白做。

### 变更 1 ★ 屏幕方向：竖屏 → 横屏

原宪法写的是「手机/平板**竖屏优先**」。参考图是 16:9 横屏，而且**上下分层的水面构图是这张图最值钱的部分**——水面线把画面分成天空和水下两半。

这个构图直接解决了我们的换气机制：

> 换气不再是"切换到另一个界面"，而是**镜头跟着小爱心往上摇**，穿过水面线，看见天空和远处的小岛。
> 换完气镜头再摇回来。**一个连续的世界，没有转场。**

竖屏做不出这个效果。**建议改成横屏 iPad。**

代价：iPad 横屏拿着比竖屏累一点。收益：整个世界观的呈现方式。我认为值。

**这条要你点头。**

### 变更 2 ★ HUD 要拆掉三样东西

参考图的 HUD 是星露谷的，直接照搬会违反我们自己的原则：

| 参考图上的 | 问题 | 处理 |
|---|---|---|
| ⚔️ 剑（道具栏第 1 格） | 我们没有战斗 | **删除** |
| 🐟 鱼（道具栏第 4 格） | 违反原则 7「野生动物不被拥有」 | **删除**，换成贝壳 |
| 💰 12,450g（左上角） | 金钱常驻会制造压力；且贝币 S5 才有 | **S5 之前不显示**，S5 后移到「换一换」界面内，不做常驻 HUD |
| 📅 Summer, Year 2 | 我们没有四季 | 改成 **潮汐 + 月相**（见下） |

**修改后的 HUD**：

```
┌──────────────────────┐
│ 🌙 满月   🌊 涨潮中     │   ← 左上，木框保留
│ 第 18 天               │
└──────────────────────┘

                                    ┌──────────────┐
                                    │ 🌱 │ 🐚 │ 🎁 │  ← 右下，只有 3 格
                                    └──────────────┘
                                      海草  贝壳  礼物
```

**道具栏永远不超过 4 格。** 星露谷的 12 格背包对 6 岁是灾难。

### 变更 3 ★ 像素风 vs 童童的手绘稿

原计划是童童画 4 张 PNG 替换占位图。现在世界是像素风，她的手绘稿放进去会很突兀。

**推荐方案：分层，让风格冲突变成设计**

- **世界 + 小爱心 + 所有 NPC** → 统一像素风
- **童童画的东西** → 全部装在"画框/纸"里出现：送给 Dolly 的画挂在潟湖边的一块板子上、图鉴的手绘页是一张纸、命名的纸条贴在海草床边

这样她的画不是"混进"世界，而是**被世界郑重地挂起来**。风格差异反而强化了"这是童童画的"。

- 另外保留一个后台开关 `"小爱心皮肤": "pixel" | "tongtong"`，随时可以把小爱心换成她画的版本。**留着，但不是默认。**

---

## 1. 视觉与技术基线

### 1.1 分辨率与缩放

| 项 | 规定 |
|---|---|
| 逻辑分辨率 | **480 × 270**（16:9），所有游戏坐标基于此 |
| 缩放 | **整数倍缩放**（×2 ×3 ×4），绝不非整数 |
| CSS | `image-rendering: pixelated`（全局，别漏 canvas） |
| 屏幕适配 | 取 `floor(min(vw/480, vh/270))` 作为倍率，居中，四周留纯色边 |
| 方向 | 横屏锁定，竖屏时显示"请横过来"提示图 |

**不要用非整数缩放去填满屏幕。** 像素画一旦被 1.37 倍拉伸就毁了，宁可留黑边。

### 1.2 图层结构（照着参考图拆）

```
z:0   天空 + 云（缓慢横移）
z:1   远景小岛
z:2   水面线（波纹动画，8fps）
z:3   水下远景 —— 深色剪影植物
z:4   水下中景 —— 珊瑚、海草（摇摆）
z:5   沙底 + 散落物（贝壳、石头、宝石）
z:6   ★ 小爱心 + NPC
z:7   气泡粒子
z:8   HUD
```

镜头只做**垂直摇移**（换气）和**水平跟随**（移动），不做缩放。

### 1.3 动画规格

| 对象 | 帧率 | 说明 |
|---|---|---|
| 小爱心 / NPC 精灵 | **8 fps** | 像素风的"顿挫感"来自低帧率，别做高帧 |
| 海草摇摆 / 珊瑚 | 8 fps 或 CSS transform | 循环，无起止 |
| 气泡 | 粒子，连续 | 从 `pet.json` 的 `blowhole` 锚点发射 |
| 水面波纹 | 8 fps | |
| 镜头摇移 | 平滑，300–500ms | 唯一允许的连续运动 |

**参考视频的运动强度是对的**：小爱心缓慢横向漂移 + 极轻微上下浮沉 + 尾鳍慢摆。不要加速、不要转身特效。

### 1.4 素材规格（更新 `pet.json`）

```json
{
  "规格": {
    "精灵尺寸": "192 x 64 像素（逻辑分辨率下,成年基准,按参考图比例定,详见 pet.json）",
    "格式": "PNG,透明背景,无抗锯齿",
    "朝向": "一律朝右",
    "调色板": "限制在 16 色以内"
  },
  "动画": {
    "idle":     { "文件": "idle.png",     "帧数": 4, "fps": 8 },
    "swim":     { "文件": "swim.png",     "帧数": 6, "fps": 8 },
    "eating":   { "文件": "eating.png",   "帧数": 4, "fps": 8 },
    "surfacing":{ "文件": "surfacing.png","帧数": 4, "fps": 8 },
    "sleeping": { "文件": "sleeping.png", "帧数": 2, "fps": 4 }
  },
  "锚点": {
    "blowhole": [0.80, 0.30],
    "mouth":    [0.86, 0.60],
    "eye":      [0.75, 0.38]
  }
}
```

精灵表横向排列，代码按帧宽切分。**锚点依然不许硬编码。**

### 1.5 技术栈变更

| 项 | 原定 | 现在 |
|---|---|---|
| 存档 | localStorage 单 key | **双存储**：localStorage 存状态 + IndexedDB 存童童的画 |
| 渲染 | DOM + CSS transform | **保持 DOM**，不引游戏引擎。精灵用 `background-position` 切帧 |
| 动画 | CSS / Framer Motion | CSS `steps()` 做精灵帧动画，Framer Motion 只做镜头摇移 |
| 方向 | 竖屏 | **横屏锁定** |

**依然不引入 Phaser / PixiJS。** 这个游戏的复杂度撑不起引擎的学习和维护成本。

---

## 2. 数据文件结构

S1 一次性把这些文件的骨架建好，后面各 Season 只往里填内容。

```
content/
├── config.json      ★ 控制后台（爸爸的旋钮）
├── world.json         海域 → 地点 → 互动点
├── npc.json         ★ 所有角色：语言/性格/喜好/话题库
├── dialogue.json      小爱心自己的台词（已有）
├── knowledge.json     图鉴知识卡（已有）
├── whitelist.json     成语 + 英语白名单（已有）
├── facts.json       ★ 新增：每条设定的真实出处
├── events.json        自然事件日历
└── economy.json       贝币与物品价格
```

### 2.1 `npc.json` schema（最重要的一个）

```json
{
  "dolly": {
    "名字": "Dolly",
    "物种": "旋转海豚",
    "学名": "Stenella longirostris",
    "海域": "红海浅滩",
    "地点": "海豚潟湖",
    "语言": "en",
    "性格": "快、跳、坐不住",
    "精灵": "/assets/npc/dolly/",
    "出现时段": ["day"],
    "关系类型": "熟悉度",
    "熟悉度上限": 5,
    "喜欢的礼物": ["会动的东西", "亮的东西"],
    "不感兴趣": ["石头", "贝壳"],
    "话题库": {
      "1": [
        { "say": "Hello! Look at me!", "audio": "dolly_hello.mp3",
          "options": [
            { "text": "Hello!", "icon": "🐬", "audio": "opt_hello.mp3" },
            { "text": "Wow!",   "icon": "👀", "audio": "opt_wow.mp3" }
          ]}
      ],
      "3": [ "..." ],
      "5": [ "..." ]
    },
    "解锁知识卡": ["enc_spinner_dolphin"],
    "factRef": "fact_spinner_lagoon"
  },

  "laohu": {
    "名字": "老虎",
    "物种": "虎鲨",
    "学名": "Galeocerdo cuvier",
    "海域": "澳洲浅海",
    "地点": "深水通道",
    "语言": "zh",
    "关系类型": "距离",
    "距离上限": 3,
    "永久锁定级": 4,
    "锁定文案": "他不会。",
    "安全护栏": {
      "永不追逐": true,
      "永不夜间出现": true,
      "永不主动接近": true,
      "随时可退": true
    },
    "默认开启": false
  }
}
```

**`关系类型` 这个字段是关键。** 它让虎鲨的"距离"和其他人的"熟悉度"走同一套代码，不需要写两遍。

### 2.2 `config.json`（控制后台）

见企划 §10.3。S1 必须实现的最小集：

```json
{
  "开放海域": ["红海浅滩"],
  "开放地点": ["家海草床", "换气水面", "海豚潟湖"],
  "开放NPC": [{ "id": "dolly", "语言": "en", "熟悉度上限": 5 }],
  "系统开关": {
    "交易": false, "委托": false, "图鉴": true,
    "信使远行": false, "虎鲨": false,
    "画画送礼": false, "英语描红": false
  },
  "语言设置": { "拼音": true, "英语朗读": true, "英语选项数量": 2 },
  "成长设置": { "阶段推进速度": 1.0, "手动设置阶段": null },
  "知识卡": { "屏蔽": ["bio_vulnerable"] },
  "陪伴设置": { "单次游玩软提示分钟": 20 },
  "小爱心皮肤": "pixel"
}
```

### 2.3 `facts.json`（真实性追溯）

```json
{
  "fact_spinner_lagoon": {
    "说法": "旋转海豚白天在红海浅水潟湖休息",
    "出处": "红海 Sataya / Samadai 海豚休息区，多个潜水与保育资料记载",
    "已核对": true
  },
  "fact_shark_bay_seagrass": {
    "说法": "鲨鱼湾海草床约 4800 km²,含 12 种海草",
    "出处": "Marine Mammal Habitat / UNESCO 世界遗产资料",
    "已核对": true
  }
}
```

**每个 NPC、每个地点、每张知识卡都要有 `factRef`。** 没有出处的设定不许进游戏。

---

## 3. Season 计划

### S0.5 · 换皮（1–2 周）★ 先做这个

**为什么排在最前**：美术方向变了、屏幕方向变了、分辨率体系变了。这三样越晚改，重构成本越高。现在代码量最小，趁现在。

**交付**：现有 S0 的全部功能，在新的像素风 + 横屏 + 分层世界里跑起来。

**状态：✅ 全部完成**(2026-07-29~30)。详细的「实际做了什么」和留下的坑见 `CLAUDE.md` 七、当前进度。

| # | Prompt | 状态 |
|---|---|---|
| 0.5-1 | 把项目从竖屏改成横屏锁定。建立 480×270 逻辑分辨率 + 整数倍缩放的渲染容器，全局 `image-rendering: pixelated`。竖屏时显示「请横过来」提示。先不动任何游戏逻辑。 | ✅ |
| 0.5-2 | 按 `视觉基线 §1.2` 建立 9 层图层结构（天空/远岛/水面线/水下远景/中景/沙底/角色/气泡/HUD）。每层先用纯色块占位，确认层级和视差正确。 | ✅ 纯色占位,真素材还没接 |
| 0.5-3 | 实现精灵帧动画组件 `<Sprite>`：读精灵表 PNG + 帧数 + fps，用 CSS `steps()` 切帧。用一张临时精灵表验证。 | ✅ |
| 0.5-4 | 把小爱心接到 `<Sprite>` 上，读新版 `pet.json` 的动画配置。保留原有的漂浮移动逻辑。 | ✅ |
| 0.5-5 | 换气改成镜头垂直摇移：小爱心游向水面 → 镜头跟着上摇穿过水面线 → 露出天空和远岛 → 点击完成换气 → 镜头摇回。全程无转场、无黑屏。 | ✅ |
| 0.5-6 | 重做 HUD：左上木框显示「月相 + 潮汐 + 第 N 天」，右下道具栏固定 3 格（海草/贝壳/礼物）。**不要金钱显示，不要剑，不要鱼。** | ✅ 月相/潮汐是占位文字,S4 才会算真的 |
| 0.5-7 | 把海草床、饱食度、剧本对话、成长面板、相册全部迁移到新布局，确认无功能丢失。 | ✅ |

**验收**：童童玩起来和 S0 一样，但世界看起来是参考图那样，且换气时能看见天空。

---

### S1 · 第一个朋友（2–3 周）

**交付**：地点系统 + NPC 框架 + Dolly（英语）+ 送礼 + 熟悉度 + **控制后台 v1**

**状态：✅ 全部完成(1-1 至 1-8)。**

**1-6 补做说明**：用户明确要求「用海草做礼物」。已长成的海草(和喂食共用同一份数量,不消耗、不扣减)当礼物,每个 NPC 每天限送 1 次(存档 `giftedAt` 记录)。反馈从 npc.json 的 `礼物反馈.很喜欢` / `礼物反馈.谢谢你` 里随机挑一档(50/50,不做偏好匹配,因为"没有拒绝没有讨厌"),没填这两个字段的 NPC(目前除 Dolly 外都还没填)有通用兜底文案。送礼和聊天共用同一条「熟悉度每天最多涨一级」的规则——同一天不管是聊天还是送礼,先到先得,不会叠加涨两级。

**1-8 说明**：只记「选了哪个选项」这类真的对话选择(NpcDialogue 里点了一个 option),送礼的反馈气泡不算对话、不进日志。存 `xiaoaixin_log`,上限读 `config.json` 的「后台.对话日志上限」(在控制后台里可改,改小了会立刻把存量截掉)。控制后台新增「对话日志」区块:显示条数 + 列表(时间 · NPC · 选了什么)+「导出 txt」按钮。

| # | Prompt | 状态 |
|---|---|---|
| 1-1 | 建立 `content/world.json`、`content/npc.json`、`content/config.json`、`content/facts.json` 四个文件的骨架，schema 照 ROADMAP §2。先只填「家海草床」「换气水面」「海豚潟湖」三个地点和 Dolly 一个 NPC。 | ✅ 开工前就已经存在(内容比要求的更全,含未来 Season 的骨架,用 `_待填` 标记),直接复用没有重做 |
| 1-2 | 实现地点系统：左右滑动或点击箭头在地点间移动，镜头水平跟随。地点是否可进入读 `config.json` 的「开放地点」，未开放的地点显示为远处的模糊轮廓，点击无反应。 | ✅ |
| 1-3 | 实现 NPC 组件：读 `npc.json`，在对应地点渲染精灵，有 idle 动画，点击可对话。先用占位精灵。 | ✅ 用的是 Dolly 已有的像素图,不是占位色块 |
| 1-4 | 实现对话系统 v2：支持按 `语言` 字段切换。语言为 `en` 时，NPC 台词和所有选项都是英文短句 + 图标，且**每句都能点击朗读**（用 Web Speech API，失败时静默降级，不报错）。 | ✅ |
| 1-5 | 实现熟悉度系统：0–5 级，**只增不减**，永不衰减。升级时解锁 `话题库` 里对应等级的新对话。熟悉度上限读 `config.json`。 | ✅ |
| 1-6 | 实现送礼：每个 NPC 每天可送 1 次，反馈只有「很喜欢」和「谢谢你」两档，**没有拒绝、没有讨厌**。送礼后熟悉度 +1。 | ✅ 用已长成的海草当礼物 |
| 1-7 | 实现控制后台：长按标题 5 秒 + 4 位数字密码进入。可视化编辑 `config.json` 的全部字段，改动存 localStorage 并覆盖文件默认值。提供「恢复默认」和「一键回到只有小爱心」两个按钮。 | ✅ |
| 1-8 | 控制后台加「对话日志」页：记录每次对话的时间/NPC/选了哪个选项，可导出 txt。存 localStorage，超过 500 条时丢弃最旧的。 | ✅ |

**验收**：
- 用后台把世界改成「只有红海 + 只有 Dolly + 只说英语」，童童能独立完成一次完整对话 —— ✅ 2026-07-30 用浏览器实际玩了一遍验证通过(地点导航、Dolly 对话、送礼、后台密码锁+设置页、对话日志都正常,全程控制台零报错)
- 用后台一键关掉 Dolly，游戏回到 S0.5 的样子且不报错 —— ✅ 同上验证通过,「一键回到只有小爱心」和「恢复默认」都正确

**验收过程中发现并修复的 bug**：`public/content/npc.json` 曾被存成 UTF-16LE 编码(不是 UTF-8),导致浏览器 `fetch().json()` 解析报错、被 `useNpcs.ts` 的 `.catch(() => {})` 静默吞掉——**实际效果是 Dolly(以及所有 NPC)在跑起来的游戏里从来没有渲染出来过,且没有任何可见报错**。`tsc`/`build`/`lint` 都不会检查 JSON 文件的字节编码,所以直到真的打开浏览器玩才发现。已转回 UTF-8 并核对内容无损。**教训**:静态内容 JSON 改完之后,光跑 build/lint 不够,得实际 fetch 一次确认能解析。

**验收过程中发现、明确要求先不修的 bug（2026-07-30 UI 重做已处理，见下方新章节）**：Dolly 的位置(`Npc.tsx` 的 `leftPercent`)和底部固定的 HUD 按钮行(🌱 喂海草/🌿 种海草)在屏幕上有重叠,HUD 在更高的 z 层(z:8 > NPC 的 z:6),重叠区域内 Dolly 和她的送礼按钮曾经**真的点不到**,不只是视觉盖住。这次按钮区缩小并挪到右下角紧凑两排,**但 `Npc.tsx` 的 `leftPercent` 本身没动，理论上仍可能在某些地点/某个 i 值下和新按钮区重叠——去海豚潟湖见 Dolly 时请实测确认**。

**已知没处理、下次可能要注意的坑**(详细说明见 `CLAUDE.md` 七、当前进度):
- ~~小爱心和 NPC 的对话没有互斥,理论上能同时弹出两个对话框~~ **已修复(2026-07-30)**：见下方新章节,小爱心和邻居现在共用同一个 `DialoguePanel` 和同一份"谁在说话"的状态,结构上不可能再同时弹出两个
- 「换一片海」（SeaButton/SeaPicker）不受 `world.json` 的「解锁阶段」限制,能随时切三片海,和 GDD 的远行叙事（海龟信使 + 诚实标注）没有对齐——**这次明确留到下一轮**
- 换气镜头摇移是全局的,不受地点限制,虽然「换气水面」在 world.json 里概念上才是触发点——**这次明确留到下一轮**
- 小金的「复读」语言类型还没有对应分支——**S3 才会有小金,不在这次范围内**

---

### S0.5+S1 · UI 统一重做（2026-07-30）

**背景**：S0.5/S1 验收时发现的按钮偏大偏丑、NPC 被 HUD 挡住,用户当时都说"先不改,等以后统一做 UI 设计"。这次就是那次统一重做,同时带上了对话互斥的遗留 bug。用户提供了三张参考图(`wiki/graphic/stardew参考/`),明确要求**这次美术还原度优先于 CLAUDE.md 里旧的美术类数值规则**(已在 CLAUDE.md 新增的「二十、UI 统一重做」章节里记录了具体哪几条被覆盖、为什么)。

**做了什么**：
- 顶部状态栏重做成参考图那种紧凑方形挂件:月相/潮汐(仍是占位)+ 第几天 + **新增体长/体重**(真实数据,见下)
- 底部按钮从散落各处的大圆按钮,收成右下角两排紧凑方形:第一排贝壳/礼物/背包(灰态占位)+图鉴,第二排喂海草/种海草/换一片海/相册/**新增的换气呼叫按钮**(该换气时会发光,不再需要点小爱心本体)
- 顺带发现并修好一个跟这次工作强相关的旧 bug：`pet.json` 换成像素精灵规格后,`成长.阶段`(按天数分阶段的数组)丢了,只剩没被读取的 `成长缩放`——**实际效果是小爱心从 S0.5 起就再也没有真的长大变色过**。已经把阶段日期(按 GDD §5.1:幼年 0-14/少年 15-40/亚成年 41-80/成年 81+)和真实体长/体重数值一起补回 `pet.json`
- 体长直接用 GDD §5.1 的区间中点(1.15/1.75/2.4/3 m)。体重只有成年有实测锚点(约 400kg),中间三档**不是编的**,是按体长比例的立方从成年锚点反推(几何相似动物,体积/质量与线性尺寸三次方成正比),`pet.json` 里写清楚了换算方法,不是查到的数据
- 小爱心新增一套「屏息待换气」像素精灵(`draw-pet.mjs` 的 `holding.png`):腮帮微鼓、嘴巴闭紧,安静漂在水面,不再靠头顶那句「我要换口气」的文字提示
- 「我有点饿了」「我要换口气」两句文字气泡整个去掉了,饿不饿看 `FullnessMeter`(已有),要不要换气看小爱心的新姿势 + 一直都有的换气泡泡,纯视觉,不用读字
- 对话从"钉在头顶的气泡"改成参考图那种从底部滑上来的木纹面板(`DialoguePanel.tsx`),小爱心和邻居共用同一个,同一时刻只可能有一份内容——顺带修掉了"对话没有互斥"那条旧坑
- 木纹/羊皮纸这套新配色是在 CLAUDE.md 原有十色之外**追加**的,没有删改原来那套海洋配色

**没做/明确留到以后**：「让它睡觉」这个反应(以前挂在"我有点饿了"气泡的第二个选项上)随着那句气泡一起消失了,现在没有 UI 入口能触发——如果还想要,需要另外找个地方挂。「换一片海」解锁阶段限制、换气镜头摇移限定地点,这两条明确留到下一轮。

---

### S0.5+S1 · 真美术落地（2026-07-30,同日第二轮）

**背景**：用户以 `/goal` 明确指定:①按参考图(`wiki/graphic/stardew参考/Gemini_Generated_Stardew Village dugong.png`)做 Stardew 风的海洋环境九层背景 + 现有功能界面 UI;②按同一张图重新设计小爱心形象;③与 CLAUDE.md 冲突时以本目标为准。

**做了什么**：
- **小爱心重画**（`draw-pet.mjs` 重写）:按参考图换成更胖圆的体型、暖灰褐调色板(#A08D7F 主色,替代原蓝灰 #8CA3B0)、背/侧/腹三段明暗+棋盘抖动过渡、大吻部肉垫+胡须点+嘴缝、双前鳍(远侧深一档)、更大的缺刻尾鳍。帧 192×64、6 套动画、导出接口都没变;`pet.json` 锚点更新到新形体。儒艮大婶复用同一套解剖,重跑 `draw-dugong-aunt.mjs` 自动继承,疤痕色同步换成暖灰
- **世界九层真素材**（新增 `draw-world.mjs`,程序化生成到 `public/assets/world/`,19 张 PNG 共约 6KB）:天空分层渐变+白云精灵、海平线+远海面闪光、棕榈小岛、白沫水面线(steps 横向漂移)、水下远景半透明剪影 tile(叠在任意海色渐变上都成立,所以三片海不用各出一套)、8 种中景装饰(粉/橙脑珊瑚、鹿角珊瑚、管海绵、紫海扇、绿疙瘩珊瑚、灰岩、海带、高海草)、金沙海底 tile+贝壳/海星/石子散落物
- **Scene.tsx 接入**:纯色占位全部换成上述素材;水面线取整 86px(原 86.4,半像素会糊);沙底从 24px 加高到 36px(海草床根部同步上移);装饰按地点格子从 3 种排布轮换,云/岛/散落物位置按格子确定性错开——同一地点永远长同一个样;海带/高海草用 CSS 摇摆,遵守 prefers-reduced-motion
- **UI 统一到木框风**:GameHud 和 ActionBar 托盘换成木框(bg-wood/border-wood-dark)+羊皮纸内芯;Book/Album 整页改羊皮纸底+木纹页头;SeaPicker 卡片、KnowledgeCard、LocationNav、NPC 送礼按钮全部统一成 parchment+wood-dark 边框(送礼按钮从 56px 白圆改成 36px 方,和 ActionBar 同款)
- **实测并修掉「Dolly 与按钮区重叠」遗留坑**:上一轮明确留了"去海豚潟湖实测确认"——实测确实重叠(送礼按钮浮在托盘上,Dolly 半截被盖)。修法是把 NPC 抬到中上层水域(`bottom-40`),纵向错开小爱心漂移带(y157-221)和右下按钮区(y162+),横向保持 65%。海豚本来就在上层游,顺便更合理

**验收**：headless Chrome + CDP 脚本实际走了一遍——开场白选项、换气镜头摇移看天空、两次右移到海豚潟湖、点 Dolly 对话英文面板、图鉴、相册、换一片海,8 张截图逐一目检,console 零报错。

**没做/留到以后**：三片海仍共用同一套珊瑚/沙底素材(只有水色渐变不同);月相/潮汐仍是占位文字(S4);AdminGate/AdminPanel 是爸爸用的,没跟着换皮。

#### 追加 · 95% 复刻二轮(2026-07-30,用户反馈第一版"还做得不够"后)

用户明确要求**对参考图整体 95% 以上细节复刻,包括 HUD 工具栏的大小尺寸和画面比例,与 CLAUDE.md 冲突时放开手脚**。据此又做了一轮:

- **水色**:红海三层水色直接按参考图取(`seas.ts`:#38a8ac/#2a8e92/#1f7a7c)——底部保持明亮青绿,不再沉到 CLAUDE.md 十色里的深藏青 #14495C。这是对「四、配色不要自行更换」的正式覆盖,依据用户本轮授权
- **前景礁按参考图比例放大 2-3 倍并成簇**:海扇 52×54(实心织物扇面+放射肋+镂空)、海带 26×100、脑珊瑚 48×30、新增紫管海绵丛/红枝珊瑚/大岩台 110×42(带苔和草簇,丘顶抬高提亮过两轮才够醒目)、鹿角加大;两个下角层层叠压,中部沙线也放小件,不再是零散小精灵
- **沙底**:加高到 40px,加中央深色沙洼(sand_patch)、蓝宝石、小灰鱼、白贝壳散落
- **描边气泡**:白圈+高光的 bubble 精灵三个尺寸,每地点 5 串慢慢升起,替代原来的小白点
- **HUD 全面像素化**:`draw-world.mjs` 新增 ui/ 目录——panel.png(6px 切片九宫格,border-image 1:1 不缩放)、slot.png/slot_red.png(26px 道具格,红框=换气呼吸灯)、13 个像素图标(贝壳/礼物/背包/图鉴/喂草/种草/地图/相机/泡泡 16px + 月/浪/尺/秤 10px)。GameHud 压成两行信息贴参考图时钟挂件比例,hotbar 格子紧挨共享边框不再套托盘,数量白字右下角;DialoguePanel/LocationNav/SeaPicker/Book/Album 返回按钮全部换成同一套面板
- 天空更饱和、双岛(大+小棕榈岛)、远海面加浪痕横线

**验收**:CDP 全流程 8 状态重跑,console 零报错;水面/潟湖/对话/图鉴/相册/换海逐张目检。

**踩坑记录**:border-image 的切片数和 CSS border-width 必须 1:1,否则边框被非整数缩放糊掉;大岩石第一版丘心太低+颜色太暗,在沙线上只露一条缝,抬丘顶+提亮两轮才达到参考图的存在感。

#### 追加 · 交互修正四则(2026-07-30,同日第三轮,按用户逐条反馈)

1. **沙地深色块 = 小爱心的影子**:参考图沙地中间那块深色不是洼地,是白天她投在沙上的影子。删掉固定的 sand_patch,改为 shadow.png(半透明深色椭圆+抖动边缘)按她的 x 坐标实时跟随,宽度随成长体型变大。
2. **"海草跟着上浮"的真相**:上浮画面里跟着浮的那五棵不是种的海草(海草床是世界锚定的,镜头上摇后在画面外),是钉在 HUD 上的**饱食度计量条**——它长得就是五棵海草。修法:镜头在水面期间把计量条藏起来(`!breath.atSurface`)。启示:把计量条做成海草形状的代价就是它会被当成世界里的海草,以后 UI 改版可考虑挪出画面中轴。
3. **点哪游哪**(新 `usePetSwim`):点击屏幕任意处,小爱心转向那边、按真实儒艮悠闲巡游速度前进(≈5 km/h≈1.4 m/s;按成年 3m=192px 的比例换算成 90 逻辑px/s,写死不做加速)。朝向由移动方向决定,点她本体不再随机转身;原来的自动左右漂移(pet-drift)整个删除。
4. **场景切换 = 自己游过去**:删掉左右箭头和地点名字牌(`LocationNav.tsx` 已删除),世界横条上镜头水平逐帧跟随她(垂直换气摇移仍保留 400ms 平滑,两层 transform 分开)。海草床/NPC 全部改为世界坐标摆放,NPC 按所住地点的格子落位,不再随"当前地点"出现消失。

**这轮踩的坑**:角色层(Actors)原来是个全屏透明 div,会把点击全部吞掉——「点哪游哪」的点击层永远收不到事件。修法是 Actors 整层 `pointer-events-none`,小爱心/热区/海草床/NPC 各自的按钮开回 `pointer-events-auto`。透明 div 也参与命中测试,这个很容易忘。

**验收**:CDP 用 Input.dispatchMouseEvent 真实点击:连点右侧从家一路游到海豚潟湖(镜头跟随、Dolly 在场)、点左侧游回家(转身朝左)、换气上浮(计量条消失、影子留在沙上),console 零报错。

### S2 · 童童的手（2–3 周）

> 这一节的标题在 2026-07-30 插入「真美术落地」几节时被顶掉过，2026-08-07 补回。
> 下面这句「为什么提前」和 2-1～2-8 一直都是 S2 的内容。

**交付**：绘画 + 手写 + 画作送礼 + 图鉴手绘页

**为什么提前**：童童刚会写字、刚有 Pencil，现在是兴趣峰值。等半年热度就过了。

| # | Prompt | 状态 |
|---|---|---|
| 2-1 | 接入 IndexedDB：建 `xiaoaixin_drawings` object store，提供 save/load/delete/list 四个方法。localStorage 主存档里只存图片 id 引用。写一个测试页验证存 20 张图不爆配额。 | ✅ 2026-08-07 |
| 2-2 | 实现绘画画布组件：`<canvas>` 1024×768 逻辑分辨率按 DPR 缩放。Pointer Events，**`pointerType === 'touch'` 时一律忽略**（防手掌误触）。压感用 `event.pressure` 控制线宽，恒为 0.5 时降级为固定线宽。 | ✅ 2026-08-20 |
| 2-3 | 画布 UI：**只给 4 种颜色 + 1 支笔 + 橡皮 + 撤销（至少 10 步）+ 重来**。不要更多工具。 | ✅ 2026-08-20 |
| 2-4 | 画画送礼：在 NPC 对话里加「画点什么送给 TA」。送出后画作挂在该 NPC 所在地点的一块木板上，**每次来都能看到**。每个 NPC 最多挂 3 张，满了旧的自动存进相册（不删除）。 |  ✅ 2026-08-21 |
| 2-5 | NPC 偶尔提起挂着的画（约 20% 概率）：「你画的那个，我很喜欢。」**不要评分、不要星级。** |  ✅ 2026-08-21 |
| 2-6 | 手写命名：种下海草、首次抵达新地点、获得特别物品时，弹出一张小纸条让童童手写。笔迹**原样存图**，永久贴在对应位置。**绝不做手写识别。** |  ✅ 2026-08-24 |
| 2-7 | 图鉴手绘页：每张知识卡左半留空白，可点「我来画」。不画也能看知识。图鉴页显示「你画了 N 张」，**不做进度条、不做完成度百分比**。 | ✅ 2026-08-24 |
| 2-8 | 相册收录所有画作和纸条，按时间排列，支持长按保存到系统相册。 | ✅ 2026-08-24 |

**验收**：童童画一张画送给 Dolly，第二天打开还挂在潟湖边。

#### 2-1 已完成（2026-08-07）· 双存储落地

`src/lib/drawings.ts`：`saveDrawing` / `loadDrawing` / `deleteDrawing` / `listDrawings` 四个方法，
DB `xiaoaixin` → store `xiaoaixin_drawings`，key `drawing_{毫秒时间戳}`，value 就是 PNG blob 本体。
`SaveData` 加 `drawings: string[]`，**只存 id**。验证页 `drawings-test.html`（dev 才有，不进 dist）。

无头 Chrome 实测 17 项全过、控制台零报错：20 张 1024×768 PNG 共 16.6 MB 存进去，
配额占用 0.2%（10 GB 配额）；读回来的图真实尺寸 1024×768；删一张只少一张；
造出来的 20 张收尾时全删干净，跑之前就有的一张没动。

**四个坑，都记在代码注释里**：

1. **写成功不能只看 `req.onsuccess`。** put 的 onsuccess 在事务提交之前就响，配额不够是在
   `tx.onabort` 上才报出来的 —— 只看 onsuccess 会把「存满了」当成「存上了」。所以一律等
   `tx.oncomplete`。
2. **`indexedDB.open()` 可能既不 success 也不 error，一直挂着**（无痕模式、另一个标签页占着）。
   挂着比失败更糟：调用方会永远停在「正在存」。加了 5 秒超时，超时就当没有 IndexedDB。
3. **id 不能直接用 `Date.now()`。** 同一毫秒内连存两张会撞 key，后一张把前一张盖掉 ——
   画不许消失（原则 10）。改成 `Math.max(Date.now(), 上一个 + 1)`。
4. **打不开数据库时不缓存那个失败结果。** 可能只是另一个标签页正在升级，下次调用该重试。

**降级实测**（不是嘴上说说）：把 `window.indexedDB` 拿掉再 import 一次，四个方法返回
`null / null / false / []`，一个错都没抛。老存档（没有 `drawings` 字段）实测原样继承：
天数、贝壳 7、熟悉度、相册、海草一样不少，`drawings` 补 `[]`。

**base64 对照**（只算不写，不拿童童的存档做实验）：这 20 张按 base64 进 localStorage 是 22 MB，
5 MB 的配额第 4 张就爆 —— 爆的时候是整份 `xiaoaixin_save` 一起丢。注意验证页故意用
60 道随机粗线画满整屏，单张 850 KB，比童童真画的画大得多，是最坏情况下的数。


#### 2-2 已完成（2026-08-20）· 绘画画布组件

`src/components/DrawingCanvas.tsx`：逻辑分辨率恒为 1024×768，后备缓冲区 `1024×768 × DPR`，
`ctx.scale(dpr, dpr)` 之后所有绘制仍用 0–1024 / 0–768 的坐标，调用方不用知道 DPR 是多少。
对外只有三个方法：`clear()` / `exportBlob()` / `getStats()`。
**颜色盘、橡皮、撤销、重来是 2-3，挂到 NPC 的木板上是 2-4，这一轮一个都没做。**

验证页 `draw-test.html` + `src/dev/{draw-test.tsx, DrawTestPage.tsx}`（dev 才有，不进 dist，
实测 `dist/` 里没有它，游戏主包里也搜不到绘画代码，包体 264.24 kB 一字节没涨）。

无头 Chrome + CDP 实测 **27 项全过 + DPR 补测 8 项全过**，控制台零报错。

**四个坑**：

1. **`image-rendering: pixelated` 会继承到画布上。** `index.css` 把它钉在 `:root`，
   而它是可继承属性 —— 童童的铅笔线会被最近邻放大成锯齿块。那条规则是给 480×270 的
   像素世界定的，绘画画布必须显式写回 `auto`。验证页**故意**引 `index.css`，
   不引的话这个坑根本不会出现，等于没测（实测 `:root` 是 `pixelated`、画布是 `auto`）。
2. **DPR 变化不能只听 `resize`。** 把窗口拖到另一块不同 DPI 的屏幕上时视口尺寸可能一点没变，
   `resize` 压根不响 —— 第一版就是这么写的，实测 DPR 从 2 掉到 1 之后后备缓冲区还停在
   2048×1536，线条从此一直是被拉伸的。改成盯住 `matchMedia('(resolution: Ndppx)')`，
   而且**每次变完要按新的 dpr 重新布一次哨**，否则只抓得到第一次。
3. **改 `canvas.width/height` 会清空画布。** DPR 一变就等于把童童画的东西擦了 —— 违反原则 10。
   现在是先拓一份快照，换完缓冲区再贴回去（实测 dpr 2→1，墨迹从 24604 变成 6518 像素，
   正好是 1/4 的 1.06 倍，画还在）。
4. **一条 path 只能有一个 `lineWidth`。** 压感要变粗细就必须把一笔拆成很多段分别 `stroke()`，
   靠 `lineCap/lineJoin: round` 让接缝看不出来。

**两个「不是拍脑袋定的」数**：压感 0→1 映射到 0.4×→1.6× 基础线宽，中点 0.5 正好是 1.0× ——
这样有压感和没压感画出来的**平均粗细一致**，换设备不会突然变粗变细。导出恒为 1024×768
（后备缓冲区在 2 倍屏上是 2048×1536，直接导出体积是 4 倍，而 S2-1 量配额用的就是 1024×768）。

**测试环境上的两个坑**：① CDP 的 `Input.dispatchMouseEvent` **不传 `force` 时默认发 0**，
而真实鼠标按 Pointer Events 规范在按下期间恒为 0.5 —— 第一轮因此误判了一条，
得显式传 `force: 0.5` 才是在测「恒为 0.5 时降级」这个真分支。
② `Emulation.setDeviceMetricsOverride` 改 `deviceScaleFactor` 时，`devicePixelRatio` 和媒体查询的
匹配状态都会真的变（探针实测 `(2dppx)` 转 false、`(1dppx)` 转 true），但它**既不派发 `resize`
也不派发 matchMedia 的 `change`**，三个计数全是 0。所以坑 2 的 matchMedia 那道哨
**在这个环境里验不到会不会自己响**，只验到了「读到新 DPR 之后重建缓冲区并保住画」这段逻辑
（手动派发 resize 触发）。如实记一笔。

**已知的正常行为，不是 bug**：手指和手掌**完全画不出东西**（实测 21 个 touch 事件、
零像素、不算一笔）。童童没拿 Pencil 的时候，画布对她是没反应的 —— 这正是 2-2 要的防误触。

**画布本身是透明的**，纸的颜色由外面那层容器给，组件只负责笔迹。导出的 PNG 也是透明底。
2-4 把画挂到木板上时要注意这一点（深色笔画配深色木头会看不清）。


#### 2-3 已完成（2026-08-20）· 画布工具栏

`src/components/DrawingBoard.tsx`：4 个颜色格 + 笔 + 橡皮 + 退一步 + 重来，**就这些**。
没有笔刷粗细滑块、没有更多颜色、没有取色器、没有图层、没有填充、没有形状、没有缩放、没有重做。
四个颜色全部取自 §四 那张表（墨色 / 粉色 / 草绿 / 海蓝），一个新色都没发明。
怎么进画板、画完送给谁是 2-4 的事，所以这个组件既没有关闭按钮也不认识任何 NPC。

**撤销改成「记笔迹 + 重放」，不是画面快照。** 快照法在 2 倍屏上一步就是
2048×1536×4 = 12.6 MB，存十步 126 MB，iPad 直接爆。现在记的是「几条线、什么颜色、
多粗、哪些点」，撤销时清空重画一遍。顺带三个好处：内存可忽略、DPR 变化时直接按新分辨率
重画（比拓位图再贴回去更清楚，位图是被放大的）、**「重来」也能变成可撤销的一步**。

**「重来」进撤销历史是我加的，不在 2-3 原文里。** 理由是原则 10：
按错一下就把她画的东西永久抹掉，这条线不能踩。实测重来之后退一步，整张画逐字节回来。

新增 4 个像素图标（`pencil` / `eraser` / `undo` / `redraw`，`draw-icons.mjs` 生成）
和 `.ui-slot-lg` / `.ui-slot-lg-active` 两个 CSS 类（复用 canvas HUD 那张 `slot_active.png`，
选中态用 `aria-pressed` 当选择器，样式和无障碍状态绑死）。

实测 **S2-3 24 项 + S2-2 回归 27 项 + DPR 8 项 = 59 项全过**，控制台零报错。

**三个坑**：

1. **循环箭头在 16×16 上画不出来。** 试了两版：环细了糊成疙瘩；环粗到 3px，
   但 `outline()` 要在两侧各描 1px 深边，16 格塞不下，放大看像个「6」。
   换成「一张新的空白纸，右上角折起来」——大块形状不怕描边，而且「换张新纸」
   对童童来说就是重来的字面意思。**16px 上没有细线的余地，宁可粗到笨。**
2. **木框面板的填充是浅棕，奶白字 `#f8e8c8` 压上去发虚。** 那个奶白是给深色木底用的
   （HUD 那边）。浅底一律墨色，和对话面板、知识卡同一套做法。放大截图一眼看得出，
   在 1 倍图上看不出来 —— **判断对比度要放大截，别在原尺寸上瞄。**
3. **验证页里绝对不能写裸的 `button {}` 选择器。** 那会把圆角和背景色套到 DrawingBoard
   的像素按钮上，而圆角正是 §二十二·3 明令禁止的。要写成 `.bar button {}`。
   否则「这一页看着对了、游戏里却是另一套」。

**测试环境上两个新坑，比前面几个都阴**：

- **Chrome 会在画布被反复 `getImageData` 之后切换光栅化路径，切换前后抗锯齿边缘不一样。**
  测试里每画一条线取一次色，结果前两条（切换前画的）和重放出来的差 **3213 个像素、
  最大差 85/255**，看着像重放算法有 bug。用同一个场景做对照实验：不回读 12 个像素、
  差 1；每画一条回读一次就是 3213 个 —— 唯一变量就是回读。**在第一笔之前先连续回读
  8 次把模式定死，live 和 replay 就是 0 个差异像素、逐字节相同。**（热身一次不够，
  切换是按回读次数触发的。）
- **`getImageData` 给的是非预乘值。** alpha=3 时每个通道的量化台阶是 255/3 = 85，
  底层差 1 个最低位读出来就是 85。要判「屏幕上看不看得出来」必须换算成预乘值再比。
  这条差点让我把一个纯粹的量化假象当成真 bug。

**还有一个环境坑**：`TaskStop` 停掉 `npm run dev` 之后，vite 的子进程可能还占着端口。
结果开了三个 dev server（5173/5174/5175），而测试打的是最早那个。
`netstat -ano | grep :517` 查 PID、`taskkill //PID <pid> //F` 清干净，再确认只剩一个。


#### 2-4 / 2-5 已完成,2-6 代码完成未实测(2026-08-21)

**2-4 画画送礼**(实测 24/24,零报错):NPC 身边多一个画画按钮 → `DrawingOverlay` →
送出后存进 IndexedDB、id 进 `drawings`、挂到 `hangings[npcId]`(最多 3 张)。
满了之后最旧那张**只从 `hangings` 挪走,不删** —— 它仍在 `drawings` 里,也就是仍在相册里。
新增 `hang_board.png`(98×29,三个 4:3 凹格)、`useDrawingUrls`(IndexedDB blob → object URL,
渲染层只认路径)、`exportBlob(background?)`(垫不透明底,否则墨色笔画压在深色木头上看不清)。

**2-5 邻居提起挂着的画**(实测 9/9):20% 概率,台词走 `npc.json` 的 `挂画反馈`,
没填的走代码兜底「我喜欢你画的」。**不涨熟悉度、没有选项、只说喜欢不评价画得怎么样。**
复用 `isGift: true` 那条路径 —— 它的实际含义是「一句不需要她回应的话」。

**2-6 手写命名**(代码完成,**浏览器实测还没跑**):`notes`(位置 key → 纸条 id)、
`visited`(去过的地点)、`note_tag.png`(36×31 带钉子)、`render/world.ts` 的 `onSpotChanged`。

**三个坑**:

1. **缩略图那一处必须开 `imageSmoothingEnabled`,别跟着世界层关。** 世界层为像素风全程关平滑,
   而把 1024 宽的画塞进 28px 的格子是 36:1 缩小 —— 最近邻等于每 36 个像素取 1 个,
   6px 的线条整条被跳过,板上三个格子是**纯白的**。用完必须还回去,否则精灵会被插值糊边。
   **这条没有任何自动检查能抓到**(木色像素数一直是 372,和画显不显示无关),只有看图才发现。
2. **邻居正说话时点画画按钮被静默吞掉。** 按钮在、能点、点了没反应 —— 这个项目最忌讳的死点击。
   改成收起台词并打开画板,和「她在说话时再点她」同一个约定。只有剧本对话仍然挡着。
3. **`allSpots` / `openSpots` 原来每次渲染都新建数组。** 2-6 加了几个吃它们当依赖的
   `useMemo`/`useCallback`,不 memo 的话依赖每帧都变,那几个 memo 等于白写。

**一处按宪法改了本文写法**:2-6 原文说三个触发点都「弹出一张小纸条」。种海草照做(她刚动完手,
是对动作的回应);**到新地点没弹** —— 她正游着,全屏弹层横插一杠正是宪法二禁止的弹窗打断,
改成在那儿摆一块空木牌,点它才写。写过的牌不给热区(原则 10:她写的不许被改掉,
也就没有「重写」)。第三个触发点「获得特别物品」**做不了**,道具系统是 S5 的事。

**测试环境上三个坑**:① **别猜选择器**,去组件里看一眼 —— 我瞎猜的 `[data-dialogue-option]`
根本不存在,开场剧本从头到尾没关掉,`sceneId` 一直为真,画画按钮点了当然没反应。
② 台词本体和选项**都是 `button.sv-plate`**,`querySelector` 永远先命中台词那个,
点它只是重读一遍、永远推进不了剧本 —— 选项带 `min-h-6`,用它区分。
③ **改了数据文件测不出效果,先怀疑浏览器缓存**:复用同一个 Chrome profile 时
`/content/*.json` 会被磁盘缓存住,服务器发新的、页面拿旧的。测试开头一律
`Network.setCacheDisabled`。

---

#### 2-6 实测 + 2-7 / 2-8 完成(2026-08-24)· S2 收工

**2-6 木牌坐标全错,是看截图才发现的**(实测 8/8)。上一轮的坐标是拍脑袋写的,三处都不对:

1. **横坐标漏了除以 100。** `plant.x` 是 0–100 的百分比,写成 `plant.x * STAGE_WIDTH`
   算出来是 2880–45120,而整个世界才 1440 宽 —— 海草名牌**从来就没进过画面**。
2. **纵坐标写在舞台坐标系里,牌子活在世界坐标系里。** 世界高 `STAGE_HEIGHT + WATER_LINE`
   = 356(多出来的 86 是水面以上的天空),`STAGE_HEIGHT - 108` = 162 换算过去是水中央。
   截图里四块木牌像画框一样挂在半空。地点牌的 `y = 52` 更远 —— 那是**水面线以上**,
   平时根本不在画面里。
3. **地点牌的横坐标钉在 +20,那一格永远是珊瑚堆。** `DECOR_VARIANTS` 三个变体的左右两角
   都塞满了岩石珊瑚(0–110 和 360–480),而装饰和牌一样是世界坐标钉死的 —— 这不是
   镜头晃一下就好的,是**永远**被埋掉大半。挪到三个变体都空着的 140–270 那一段。

**摆位改成渲染层算**(`seagrassNoteRow` / `placeNotePos` 在 `layers/actors.ts`)。
沙面在哪、海草长多高,`drawSeagrassBed` 本来就知道;App 那边再算一遍就是把同一套几何抄两份,
抄错了没有任何自动检查能发现 —— 上面三条全是这么来的。

**两个只有跑起来才暴露的 bug,都不在这一轮的任务里,但不修 2-6 就是坏的:**

* **家海草床那块牌一整局都不会出现。** `setSnapshot` 结尾会**同步**画一帧,而 React 那边
  effect 的顺序是先 `setSnapshot` 后 `onSpotChanged` —— 第 0 格的 `-1 → 0` 就发在那一帧,
  回调还没挂上,而 `spotIndex` 已经落定,**永远不会再发第二次**。修法是订阅时补一次
  (`onSpotChanged` 里 `if (this.spotIndex >= 0) cb(...)`),这是「晚订阅的人也要收到当前值」
  的标准写法。App 那边同时改成「记下格号,等地点表加载完再对」——
  第一帧发出来的时候 `config.json` / `world.json` 多半还没回来,当场查表也是查不到的。
* **`useConfig` 每次渲染都返回一个新的 config 对象。** 上一轮特地给 `allSpots` / `openSpots`
  加的 memo 因此**全部被架空**(依赖每帧都变)。平时只是白算,这次依赖链上一加 `setState`
  就直接变成 `Maximum update depth exceeded`。合并结果加 `useMemo` 修掉。

**一处设计决议(用户当场拍板)**:海草名牌**只给写过名字的那几棵**立。空牌也立的话,
种满 12 棵时 36px 宽的牌铺满整条沙线连成一道栅栏,把海草床整个挡住 ——
实测截图里「小尾巴」被「家」盖掉了半块,**盖住的是童童写的字**(原则 10)。
代价是种下时点了「先不写」这棵就没法补名字了,用户接受。
配套加了一道躲让:后摆的牌先试叶子右边,右边占了翻到左边,两边都占了才贴着往右排 ——
挨得近的两棵海草也不会互相压字。

**空木牌从「黑洞」改成「一张白纸」。** `note_tag.png` 的凹槽原来是深棕(64,40,22),
在亮沙地上是整屏最重的一块,一排下去像几个窟窿。改成纸色(255,253,246),
和 `DrawingOverlay` 导出时垫的底同一个值 —— 写过之后那张画正好铺满凹槽,亮度不跳。
**这条同样是截图才看得出来的,数字全绿的时候它一直是「对」的。**

---

**2-7 图鉴手绘页**(实测 10/10):每张知识卡左半是一块 4:3 的木框,里面要么是她画的,
要么是空白的纸 +「我来画」。**右边那段字和这块空白没有任何关系,不画不会少一个字。**
画过之后那块不再是按钮(原则 10:没有「重画一张」)。图鉴顶上只有一句「你画了 N 张」,
没有进度条、没有百分比,**一张都没画时那句话根本不出现** —— 挂个「你画了 0 张」
出来就是个零分的记分牌(原则 4)。存档新增 `cardDrawings`(知识卡 id → 画的 id)。

**2-8 相册收录**(实测 9/9):`drawings` 全集按时间排,每张标出来处、写上日期。
日期直接从 id 里读(`drawing_{毫秒}`),不另存一个会和 id 对不上的字段。

* **来处必须单独存,不能从 `hangings`/`notes`/`cardDrawings` 反查。** 挂满 3 张之后
  最旧那张会从 `hangings` 里挪走(2-4),挪走之后就再也查不出它当初送给了谁 ——
  而它还在相册里,总不能写「不知道哪来的」。新增 `drawingOrigins`(画的 id → 位置串),
  存的是 key(`npc:dolly` / `seagrass:xx` / `place:xx` / `card:xx`)不是现成的句子,
  邻居和地点的名字将来改了,相册里跟着改。老存档里的画没有来处,就只写日期,不编一个出来。
* **长按存到系统相册靠的是「它是一个真 `<img>`」**,不是靠代码。所以那张图必须是
  `<img>` + object URL,不能换成 canvas 或 `background-image`(那两样长按都没有系统菜单),
  也不能给它加 `-webkit-touch-callout: none`。测试里断言的就是这三条;
  **iPad 上真的长按存下来这一步,这台机器验不了,如实记着。**
* **图鉴/相册没打开就不读画。** 一张 1024×768 的 PNG 几百 KB,一开局把全集读进内存
  在 iPad 上是白扔 —— 改成按当前页要哪批读哪批,关掉页面 object URL 一起收回去(实测)。

**顺带改的两处文字**:画板的退出按钮原来一律写「先不写」,画画的两个入口改成「先不画」
(按钮上写着「我来画」、退出去却写「先不写」,对着正在认字的孩子是两回事)。

**测试环境上又一个坑**:**种存档要等 app 先稳定下来再种。** 它 mount 之后还会自己写几次
存档(config 加载完、visited 记一笔……),种子写在那之前会被原样盖掉 ——
第一次跑过、第二次跑挂,查了半天才发现是竞态不是代码。开头一律先 `sleep(2500)` 再种。

四套实测(2-6 八项 / 2-7 十项 / 2-8 九项 / 回归十项)共 **37 项全过,控制台零报错**,
回归在**开发构建和生产构建各跑一遍**。`npm run build` 通过;`npm run lint` **24 problems**,
与开工前完全一致。

#### 系统开关接上代码(2026-08-24,同日追加)

`config.json` 的 `系统开关` 里,「图鉴」「画画送礼」「手写命名」三个**从来没有任何地方读过** ——
爸爸在后台拨一下,游戏里什么都不会变。「按钮在、点了没反应」和「开关拨了没变化」是同一类死控件,
所以一起接上(实测 12/12,开发构建和生产构建各跑一遍)。**另外新加一个「图鉴手绘页」**
(2-7 原来没有开关,用户确认单开一个,实测 9/9)。

其余七项(自然事件/委托/交易/三个罐子/英语描红/信使远行/虎鲨)对应的系统还没做,
**开关先占着位,不是死控件**,不用管。虎鲨那条按宪法十七永远默认 `false`。

**接上的同时把 `画画送礼` / `手写命名` 从 `false` 改成 `true`。** 这两个默认值是 S1 时代留下的
(`_当前状态` 那行原文就写着「S1 目标状态」),S2 做完之后再照原样接上去,
等于把刚做完的 2-4 / 2-6 一起关掉 —— 那显然不是「接上」的意思。

**关掉 = 不再出新的,不是抹掉已有的。**

| 开关 | 关掉之后 | 已经有的东西 |
|---|---|---|
| 图鉴 | 底部那一格空着(不做成灰格子+「长大了才有」,那句话是假的:它不是没长大,是爸爸关了) | — |
| 图鉴手绘页 | 知识卡不再出「我来画」,**没画过的那张卡整块左半不出现、回到纯文字**(不是留一个空框在那儿) | 画过的那几张照旧贴在卡上,「你画了 N 张」照旧显示 |
| 画画送礼 | 邻居身边没有画画按钮,送海草按钮**顶上来占那个位置** | 挂在木板上的画照旧挂着,2-5 照旧会提起它 |
| 手写命名 | 不再摆空木牌,种下海草也不再弹纸条(海草照样种下去) | 写过名字的木牌照旧立着 |

最后一列是原则 10:**爸爸关一个开关,不该让童童画过写过的东西从世界上消失。**
相册照旧收全集,一张不少(实测)。

**`featureOn()` 对两种「没写 true」分开处理**:config 还没加载完当作关(否则按钮会先闪出来再收回去);
加载完了但没有这个键当作开(爸爸手改 `config.json` 时删掉一行,不该整块功能凭空消失)。

**顺手修掉一个只有几百毫秒的窗口,但后果是原则 10**:木牌原来靠 `src` 判空 ——
而图是从 IndexedDB 异步读的,读到之前 `src` 还是 `null`,**写过的牌在那一小段时间里是可点的空牌,
点下去就把她写的字覆盖掉了**。`NoteView` 加了个 `written` 和 `src` 分开,判据换成它。

**为什么单开一个键而不是并进「画画送礼」**:那个开关的名字管的是「送给邻居」那条路。
爸爸关掉它是不想让童童再往 Dolly 墙上挂东西,不见得是不想让她在图鉴上画画 ——
两件事分开控制。新键排在「图鉴」后面(它是图鉴的一部分);**图鉴整个关掉时手绘页自然也进不去**,
因为根本没有入口,不是留一个点不动的按钮(实测)。

---

### 渲染重构 · 阶段 0–3 前置（2026-08-03，一整轮）

不属于任何 Season，是把渲染层从 DOM 换到 canvas 的准备与第一块落地。全过程记在 `REFACTOR_PLAN.md`，复盘和宪法修订拟稿在 `FORJake.md`。

| # | 做了什么 | 产物 |
|---|---|---|
| 1 | 盘代码、写重构方案 | `REFACTOR_PLAN.md`（五阶段，含 State 100% 不变的四道闸） |
| 2 | 从参考图提取 HUD 规格 | `wiki/graphic/hud-9slice.json`、`palette.json`、`hud-prototype.html`、`ui-proto/` |
| 3 | 精灵表处理工具 | `tools/process_sprites.py` |
| 4 | **九宫格 UI 层进 canvas**（阶段 3 主体） | `src/render/`、`src/ui/` |
| 5 | 修 `sw.js` 缓存 bug | 离线缓存此前从未生效过 |
| 6 | oxlint → ESLint | `eslint.config.js` |

**关键决定（用户 2026-08-03 拍板，三条都推翻了原有条款，宪法待修订）**

* **HUD 进 canvas** —— 推翻二十一·4「HUD 不进 canvas」。配套两条不可省：UI canvas 用**设备分辨率**后备缓冲区（否则中文糊成马赛克）；canvas 上方铺**透明 DOM 热区**（否则 `aria-label`／焦点框／Tab 全丢）
* **常驻贝壳 HUD** —— 推翻十六「❌ 常驻金钱 HUD」。`SaveData` 新增 `shells`，这同时打破了 `REFACTOR_PLAN` §四闸 2「一个字段都不加」
* **全量换 Zpix 像素字体** —— 推翻四的字体规定。Zpix 是 12px 点阵字，字号必须取 12 的整数倍，**这条反过来定死了 HUD 的布局尺寸**

**没做的（阶段 1／2／4／5）**：世界背景层、角色层、位移搬出 React、拆旧渲染、视差与光照。世界仍是原来的 DOM 九层，`Scene.tsx` 一行没动。

**双轨开关**：`?hud=dom` 退回旧的 `GameHud` + `ActionBar`，两个旧组件都还留着，阶段 4 再删。

**验收**：无头 Chrome 实测——canvas `width="1440"` 而 CSS `width:480px`（设备分辨率缓冲区生效）、10 个热区全带 `aria-label`、控制台零报错。

---

### 渲染重构 · 阶段 1–2：整个世界进 Canvas（2026-08-04）

背景六层和角色层全部搬进 canvas，小爱心的位移和镜头搬出 React。`Scene.tsx` 那套 DOM 九层退役。

| 层 | 从哪来 | 到哪去 |
|---|---|---|
| z0–z5 天空/远岛/水面/水下远景/中景/沙底 | `Scene.tsx` 六个函数 | `render/layers/background.ts` |
| z6–z7 小爱心/影子/海草床/邻居/环境气泡 | `Pet` `Npc` `Seagrass` `Bubbles` | `render/layers/actors.ts` |
| 位移与镜头 | `usePetSwim`（每帧 setX）+ `App.tsx` 的 cameraX | `render/swim.ts` `render/camera.ts`（实例字段） |
| CSS keyframes × 8 + transition × 3 | `index.css` | `render/world-data.ts` 的 MOTION + `render/easing.ts` |

**结果**：游动时 App 重渲染 **60 次/秒 → 0 次**；画面与旧渲染差 **0.4–0.7%**，且逐块扫描
无任何位置偏差（剩下的全是旋转/矢量/非整数缩放的光栅化差异）；巡游速度仍是 90 逻辑px/秒。
功能冒烟 18 项在开发和生产两套构建上各跑一遍，18/18，控制台零报错。

**顺带**：`prefers-reduced-motion` 从 CSS 媒体查询改成运行时开关（世界离开 DOM 之后媒体查询管不着它了）；
`Scene.tsx` `Pet.tsx` `Npc.tsx` `Seagrass.tsx` `Bubbles.tsx` `usePetSwim.ts` 六个文件删除。

**逐像素比对逮到的两个坑**（都是「CSS 默认值和直觉不一样」，看代码发现不了）：
`background-position: bottom` 展开是 `50% 100%`，横向那个 50% 让 repeat-x 的起点挪到了
`(容器宽-图宽)/2`，剪影层因此整体错 120px；`<button>` 会把内容垂直居中（Blink 固有行为，
不是哪条 CSS 写的），`min-h-14` 撑高按钮时精灵要跟着往下让，Dolly 因此高了 4px。

细节、验收数据和三处偏离原计划的说明见 `REFACTOR_PLAN.md` §九。当时只剩阶段 5（视差与光照），**已于次日做完，见下一节**。

---

### 渲染重构 · 阶段 5：视差 + 前景遮挡 + 镜头前瞻 + 浮尘光斑（2026-08-05）

**渲染重构到此收工。** 前四个阶段的验收标准是「画面看不出区别」，这一阶段才允许变好看，
所以四项分四步做、每步停下来给用户看过再走下一步。

| # | 做了什么 | 落在哪 |
|---|---|---|
| 1 | 视差：天空 0.15 / 远岛 0.30 / 水面 0.60 / 剪影 0.75，中景和沙底留 1.00 | `render/parallax.ts` + `world-data.ts` 的 `PARALLAX` |
| 2 | overlay 第三块 canvas：前景礁（珊瑚丛 + 会摇的高海带）压在小爱心前面；水下光柱 | `render/layers/overlay.ts`、`draw-world.mjs` 新增 7 张 PNG |
| 3 | 镜头前瞻 72px + 跟随半衰期 0.14s | `render/camera.ts`、`swim.ts` 加 `vx` |
| 4 | 远近两层浮尘 + 水面光斑 | `render/layers/particles.ts` |

**结果**：向右巡游时她坐在屏幕 x 186（不再死钉中线 240），停下来回中，速度仍是 90 逻辑px/秒；
`round(camX)` 方向反复 2/1321 帧（就是掉头那下），无抖动。小爱心游过前景海带时会从它后面过 ——
这是这一版最硬的深度信号，比背景视差明显得多。

**四条实测逼出来的修正**（每一条都是照计划做完、上屏一看不对才改的）：

1. **中景装饰不能吃视差。** `REFACTOR_PLAN` §2.4 拟的是 0.92，但 `DECOR_VARIANTS` 是**按一格画面构图**的
   （珊瑚全挤在左右两角，中间留给小爱心和海草床），慢 8% 之后格子边界和画面边界错开，
   实算第 4 格会出现两簇叠在画面左半边、右边整片空。远景那几层不吃这亏：要么是平铺纹理，
   要么是每格均匀一两个。**判据是「这层是按格构图还是均匀铺开」，不是「它有多远」。**
2. **光柱不能画在小爱心前面。** 按计划放 overlay，结果整屏白带子、连她都被洗白。
   光是水体的一部分 —— 挪到背景层最后一道（照在沙上，压在她后面），alpha 从 0.45–1.0 压到 0.22–0.5。
3. **前景礁一屏只能一丛。** 参考图是静态图，两丛在左右角框住画面；放进滚动的世界里，
   那个密度会让礁丛坐在**家海草床**上。海草种在第一格的 x 6%–94%（`seagrass.ts`），几乎铺满整屏。
   等三天才长成的东西不能被石头盖住。实测 12 棵满档，一棵没挡。
4. **珊瑚不能靠调大生成器的尺寸参数。** staghorn 枝粗上限 3px、脑珊瑚沟回间距 3.4px 都是写死的，
   尺寸翻倍之后细节密度跟不上，红珊瑚变成光秃秃一棵树。回头看参考图，前景珊瑚**也不比中景大多少**，
   是「多、挤、叠」才显得近。真正撑高度的是海带 —— 细、高、不挡视线。

**验收**：`prefers-reduced-motion` 下粒子和前景礁照画但一动不动（4 秒后两块 canvas 指纹完全一致），
镜头回到死钉中线、无前瞻；浮尘绝不漏到水面线以上（实测 0 像素）；开发/生产构建各跑冒烟，
控制台零报错；`npm run lint` 24 problems，阶段 5 全程一条没加。

细节和验收数据见 `REFACTOR_PLAN.md` §九。**渲染重构全部完成。**

---

### S3 · 红海全开（2–3 周）

**交付**：珊瑚花园、沙地浅滩、老沉船 + 4 个 NPC + 图鉴系统 + 委托

| # | Prompt | 状态 |
|---|---|---|
| 3-1 | `world.json` 补齐红海全部 6 个地点及其互动点。`facts.json` 同步补齐每个地点的出处。 | ✅ 2026-08-24（数据本来就齐，实际做的是**让六个地点在游戏里各自长得不一样**，见下） |
| 3-2 | `npc.json` 补齐：绿绿（绿海龟·中文·信使）、小金（幼年金鲹·复读）、阿玳（玳瑁·中文·讲故事）、砗磲奶奶（巨砗磲·中文·图鉴入口）、小丑鱼一家。 |
| 3-3 | 实现小金的复读机制：童童选了什么，小金就跟着说一遍（中英都支持）。这是练发音的安全出口，**不判断像不像**。 |
| 3-4 | 实现砗磲奶奶的「问问题」：从 `knowledge.json` 白名单里选。白名单外一律回答「这个我也不知道，我们去问爸爸」——**这是功能，不是失败**。 |
| 3-5 | 图鉴系统：按海域分页，每张卡含 childText / 英文（如有）/ 手绘位（**不做拼音** —— 2026-08-21 用户确认，与 CLAUDE.md 原则 2「不做拼音，中文下方那行小字的位置留给英文」一致；本行原写「拼音」是早于该条的旧文）。被 `config.json` 屏蔽的卡不出现。 |
| 3-6 | 委托系统：每天最多 1 条，出现在绿绿那里。**无时限、无失败**，不做就一直挂着。完成给熟悉度 + 图鉴卡。 |

**验收**：童童能说出至少 3 个邻居的名字，并知道谁说英语谁说中文。

#### 3-1 已完成（2026-08-24）· 六个地点各自的样子

**开工先核对，结论是数据早就齐了**：`world.json` 里红海 6 个地点连同各自的互动点全在，`facts.json` 交叉核对**零缺失**（所有被引用的 `factRef` 都有定义；反过来有 4 条定义了没人用：`fact_coral_spawning` / `fact_cowrie_currency` / `fact_shark_bay_heatwave` / `fact_spinner_length`）。所以这一轮真正做的是把它们**变成六个看得出区别的地方**，并把「开放地点」扩到 6 个（S3 的交付就是「红海全开」）。用户当场拍板本轮只做辨识度，**互动点点击出知识卡留到下一轮**。

* **装饰改成按地点 id 认，不按格子号认**（`world-data.ts` 新增 `SPOT_DECOR` + `decorFor()`）。原来是 `DECOR_VARIANTS[i % 3]`，三套排布按格子轮换 —— 三个地点时看不出问题，六个地点一开就是珊瑚花园和家海草床一模一样。**更要命的是格子号会变**：「开放地点」是爸爸在后台随手拨的，关掉一个海豚潟湖，它后面每个地点的珊瑚都会换一批，`REFACTOR_PLAN` §2.5 那条「同一个地点永远长同一个样」按格号取模根本守不住。实测（后台关掉换气水面 → 珊瑚花园从第 4 格挪到第 3 格）：它还是它自己那批珊瑚。
* **六个地点的性格照 GDD §3 的红海地点表定，一个字没自己加**：换气水面「抬头看天」→ 最空、中间什么都没有；珊瑚花园「五颜六色的珊瑚、小鱼群」→ 最挤，两丛小鱼群 + 两株海葵；沙地浅滩「挖沙、找贝壳」→ 沙洲和一路撒开的贝壳堆，珊瑚几乎没有；老沉船「长满珊瑚的旧船」→ 船是主角。**家海草床原样不动**，那是童童最熟的一格。
* 新增 6 张素材（`draw-world.mjs` 程序化生成）：`anemone` `fish_school` `shell_pile` `sand_mound` `wreck_hull` `wreck_mast`。
* **老沉船不许往恐怖里画**（宪法二）：暖棕木色 + 黄铜舷窗里是发亮的青玻璃（不是黑窟窿）、甲板上长着和珊瑚花园同一批珊瑚、断口是参差的木茬。**断桅的横桁只往一边垂** —— 第一版对称地穿过桅杆，上屏一看是个十字架，沉船旁边立十字架就是墓碑。
* 三处画完上屏才发现不对、返工的：① 海葵画了三版 —— 触手从同一点射出去会被描边糊成一块粉扇贝，加个椭圆口盘立刻变成蘑菇；30px 见方只能读出「一排分得开的手指」，根根留 2px 空让描边吃成深色缝。② 船首第一版是斜线，读出来是块搭板；上缘弧着收**并且龙骨也翘起来**，船头才是离开沙面的一个楔子。③ 沙地浅滩第一版只摆两堆贝壳，上屏是两个米粒，「找贝壳」在画面上根本不成立，改成一路撒过去。
* `config.json` 的「开放地点」扩到 6 个，并加了 `_开放地点说明`：GDD §5.1 把沙地浅滩和老沉船排在少年期，而代码不看成长阶段 —— 这一行就是那个开关。**注意 localStorage 的后台覆盖层会盖住文件默认值**，之前在后台改过开放地点的机器要重新勾选或按「恢复默认」。

**实测**（无头 Chrome + CDP，开发构建和生产构建各一轮）：六个地点逐个把镜头停在正中截图看过，控制台**零报错**；后台关掉一个地点后珊瑚花园仍是自己那批装饰。`npm run build` 通过；`npm run lint` **24 problems**，与开工前一致。

**基线比对**：改渲染之前先把家海草床那一格的旧画面存成基线（`__renderFreeze(0)` 钉死时钟），改完再截一张逐像素比 —— 差异 **0.1265%，全部落在画面最右边 7 个逻辑像素**（bbox x 1419–1439 / 3 = 473–480），那是**邻格换气水面的装饰探进来的一角**，家海草床自己的每一个像素都没动。
* **踩的坑记一条**：CDP 截出来的 PNG 是 `colorType 2`（RGB，3 字节/像素），而项目自己生成的素材是 `colorType 6`（RGBA，4 字节）。按 4 字节硬解 RGB 图不会报错，出来的是一片乱码 —— 而且**差异百分比也是假的**（第一版算出 0.353%，还煞有介事地报了个「全宽横带」的 bbox）。解 PNG 一律先读 IHDR 的颜色类型。

**没做的，如实记着**：互动点（脑珊瑚 / 海葵 / 挖沙 / 船身…）**仍然没有任何代码读**，点不动。要让它们出知识卡还缺内容 —— `knowledge.json` 现有 32 张卡没有一张挂在这些物体上，而新卡的「已核对」只有爸爸能设 true，未核对的卡代码不得展示。另外 `fact_red_sea_wreck` 和 `fact_giant_clam` 两条的「已核对」是 **false**，备注分别写着「确认具体是哪艘船 / 是否适合讲给童童」和「找一个更硬的来源」—— 老沉船这张画是那条未核对设定的视觉版本，爸爸核对时一并看一眼。

---

### S4 · 潮汐与月亮（2 周）

| # | Prompt |
|---|---|
| 4-1 | 时间系统：跟随真实系统时间。实现昼夜（天空颜色 + 光照层）、月相（30 天周期）、潮汐（一天两次，**每天比前一天晚约 50 分钟**——这是真实的，要准）。 |
| 4-2 | 潮汐影响玩法：退潮时沙地浅滩露出更多可拾取物；涨潮时可游到平时去不了的高处。**不做惩罚**，只是不同。 |
| 4-3 | HUD 月相/潮汐显示接真实数据。点击图标弹出对应知识卡。 |
| 4-4 | 事件系统框架：读 `events.json`，按月相/日期/海域触发。事件有独立的一次性演出，看完存进相册。 |
| 4-5 | 实现「珊瑚产卵夜」（满月后触发，红海）和「海龟孵化夜」（夏季某夜）两个事件。 |

**验收**：童童知道"今天月亮圆，所以潮水大"。

---

### S5 · 贝壳当钱用（2 周）

| # | Prompt |
|---|---|
| 5-1 | `economy.json`：物品清单 + 固定价格。**价格永不波动。** |
| 5-2 | 三个罐子 UI：🛍要花的 / 🏦存起来 / 🎁送朋友。捡到贝壳时童童**自己选**放哪个罐子。 |
| 5-3 | 「存起来」满 20 个可兑换平时换不到的东西（延迟满足）。**罐子里的贝壳不能互相挪动**——这是这个机制的全部意义。 |
| 5-4 | 「换一换」界面（砗磲奶奶处）：明码标价，**不做讨价还价、不做价格波动、不做负数**。 |
| 5-5 | 不可交易物品：朋友送的、童童画的、图鉴、相册，一律标 🚫「这个不换」，点击给出一句解释。 |
| 5-6 | 珍珠爷爷提前登场（可放在红海沙地浅滩作为访客），讲他那四句话。 |

**验收**：童童捡到贝壳时会停顿一下决定放哪个罐子。

---

### S6 · 远方的消息（3–4 周）

| # | Prompt |
|---|---|
| 6-1 | 信使系统：绿绿每周带来一条远方消息。达到条件时触发「远行」。 |
| 6-2 | 远行转场：加载画面**固定显示**「🐢 真的儒艮不会游这么远。这是小爱心的想象。」——**这行字不许删、不许改、不许配置关闭**。 |
| 6-3 | 印度海湾全境：红树林边、珍珠滩、咸水浅湾、越冬聚集地、贝壳堆 5 个地点 + `facts.json`。 |
| 6-4 | NPC：珍珠爷爷（定居）、驼背豚 Sousa（英语·慢·长句）、招潮蟹兄妹、火烈鸟。 |
| 6-5 | 「儒艮大集会」冬季事件：上百头儒艮聚成一群。做**估数**互动（"你猜有多少"，无对错）。 |
| 6-6 | 「回家」按钮：任何海域任何时刻可用，一键回红海。 |

---

### S7 · 最古老的朋友（3–4 周）

| # | Prompt |
|---|---|
| 7-1 | 澳洲浅海全境：乌拉梅尔草原、叠层石浅滩、海豚湾、贝壳海滩、深水通道。 |
| 7-2 | 石头爷爷（叠层石）：**每天只说一句话**，这个限制本身就是内容，不要绕过。 |
| 7-3 | Sponge（宽吻海豚·英语）：会用海绵护鼻觅食。做成可以学会并带回红海用的一个小机制。 |
| 7-4 | 儒艮大婶 + 成年演出：小爱心第一次见到很多同类。这是全游戏情感高点，**慢一点，别赶**。 |
| 7-5 | 老虎（虎鲨）：实现「距离」关系类型。四级距离条，**第 4 级永久锁定，点击显示「他不会。」** 全部安全护栏按补充文档 A.5 写死在代码里，不做成可配置项。 |
| 7-6 | 虎鲨默认 `false`。控制后台单独一个开关。上线后你先自己走一遍全部 15 句台词。 |

---

## 4. `CLAUDE.md` 需要追加的章节

S0.5 开工前，把这些写进项目宪法：

- **十二、屏幕与渲染**：横屏锁定、480×270 逻辑分辨率、整数倍缩放、`image-rendering: pixelated`
- **十三、像素美术规范**：精灵尺寸、16 色调色板、8fps、精灵表切帧方式
- **十四、双存储**：localStorage 存状态、IndexedDB 存图片。**明确禁止把 base64 图片塞进 localStorage**
- **十五、原则 6–10**：变化可逆 / 野生动物不被拥有 / 语言是角色属性 / 真实出处 / 手写不判对错
- **十六、新增禁止清单**（企划 §12 全文）
- **十七、虎鲨安全护栏**：补充文档 A.5 逐条抄进去，标明「不可配置」

---

## 5. 三个刹车

**1. 每个 Season 只做一个新系统。**
S1 只做一个 NPC。把框架磨透，后面 13 个都是填 JSON。想在 S1 顺手把 5 个 NPC 都做了，S3 会推翻重来。

**2. 控制后台在 S1，不许往后拖。**
它是你唯一能收住这个项目的东西。内容一旦多起来，没有开关就只能靠删代码。

**3. 每个 Season 上线后先自己玩三天。**
特别是 S7 的虎鲨。你觉得没问题再交给童童。

---

## 6. 现在就能做的两件事

1. **确认变更 1（横屏）**——这条定了 S0.5 才能开工
2. **把参考图切成图层素材**——天空/云/远岛/水面/远景植物/珊瑚/沙底/散落物。这批切完，S0.5 的 0.5-2 就能直接接真素材，不用占位色块

---

**下一步**：横屏这条你点头，我把 `CLAUDE.md` 的追加章节（十二到十七）和 `npc.json` / `config.json` / `world.json` 的完整初始文件一次性给你，你放进项目就能发 0.5-1。
