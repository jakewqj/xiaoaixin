# FORJake · 恢复一个被打断的任务:点哪游哪改成二维(2026-08-06)

> 上一份复盘(整个世界搬进 Canvas,2026-08-04)在 git 历史里。
> 这一轮的特殊之处:我不是从零开始做,是接手一个中途断掉的任务。所以先说接手。

---

## 一、接手时它其实已经写完了 —— 断的不是「写」,是「验」

工作区四个文件是改过的:`swim.ts` `world.ts` `layers/actors.ts` `WorldCanvas.tsx`。
我第一件事不是读代码,是跑 `tsc --noEmit`。**过了。** 再跑 `npm run build`,也过了。

这一下就把问题的性质定下来了:代码是完整的,不是写到一半停在语法错误上。
中断发生在「写完了但还没验」这个位置。所以我这一轮的活是**验证 + 记录**,不是继续写。

**可迁移的一条**:接手断掉的工作,先跑编译器,别先读 diff。编译器一秒钟就能告诉你
「这是半成品」还是「这是没验的成品」,这两者接下来要做的事完全不一样。读 diff 读半天,
你还是不知道它跑不跑得起来。

---

## 二、我误判的第一件事:把一个正常的字节当成了中断损坏

`grep` 报 `src/render/world.ts` 是 **binary file**。我当时的第一反应是:找到了,
写文件写到一半被打断,留了个坏字节。

扫下来确实有一个 NUL 字节,在这行里:

```ts
const key = this.spots.map((s) => `${s.id}\0${s.label}`).join('|')
```

差一点就当成 bug 修掉了。**幸好多跑了一步** —— 拿 `git show HEAD:src/render/world.ts`
一比,发现 committed 版本里同一个位置也有,一模一样。它是**故意**的:用一个 id 和 label
里都不可能出现的字符做分隔符,这样热区集合变没变才判得准。

**可迁移的一条**:怀疑文件被损坏的时候,**先和 HEAD 比,再下结论**。
「看起来不对劲」和「这一轮改坏了」是两回事,而你要修的只有后者。
我要是直接把 `\0` 换成 `|`,就是在修一个不存在的 bug,顺手引入一个真的。

---

## 三、我误判的第二件事,浪费了两轮:**你在我开的浏览器窗口里点**

这个值得单写一节,因为它太隐蔽了。

我起了一个带远程调试的 Chrome 去实测。测着测着发现:她会**自己动**。
我明明一次点击都没发,位置从 (260,175) 漂到了 (391,242)。

我先怀疑代码里有自主游动、有 remount、有定时器。查了一圈都没有。
于是我写了个 90 秒的纯观察脚本 —— **一动不动,完全稳定**。前后矛盾。

最后是给页面装了个事件监听,把每一次 pointer/click 连坐标和 `isTrusted` 都记下来。
一跑就现原形了:

```
{"t":"click","x":930,"y":504,"trusted":true}   ← 我没发过这个坐标
{"t":"click","x":871,"y":215,"trusted":true}
{"t":"click","x":448,"y":23,"trusted":true}
```

`isTrusted: true` = **真人的手**。那个 Chrome 窗口就开在你桌面上,你在里面玩。

**可迁移的两条**:

1. **自动化实测一律用 `--headless=new` 另起一个实例。** 有头窗口开在用户桌面上,
   它就是共享资源。我换成 headless 之后,同样的观察跑了 167 秒零位移。
2. **两次实验结果打架的时候,别急着挑一个信 —— 去测「是谁在动手」,不是「它动没动」。**
   我在「她会不会自己动」上耗了两轮,真正一击命中的是那个记录输入来源的监听器。
   问对问题比多跑几次重要。

顺带还有两个小的:**舞台几何要运行时量,别写死**(headless 和有头的布局不一样);
**点之前先 `elementsFromPoint` 确认那点没被 DOM 挡住**,而且目标坐标要按当前镜头的
可视范围算 —— 镜头一跟随,世界坐标换算出来的屏幕位置很容易跑到屏幕外(我有两次点到了 x=−228,
自己还纳闷为什么没反应)。

这条和上一轮那条「点哪要当成需要论证的参数」是同一个家族的教训,只是这次的干扰源在窗口外面。

---

## 四、代码本身,几个我复核下来觉得做对了的决定

这些不是我写的(是被打断那一轮写的),但我逐条验过,值得记下来。

### 1. 斜着游必须归一化方向

最容易写错的写法是横竖各给 90 px/s。那样斜着游的合速度是 √2×90 ≈ **127**,
比直着游快 41%,一眼看得出她在赶路 —— 而这只儒艮的人设就是「不紧不慢」。
实测归一化之后合速度稳在 **90.0**,vx/vy 比和目标方向比都是 2.53,分毫不差。

### 2. 站着换气的高度是**算**出来的,不是拍出来的

`standTop()` 去读 `pet.json` 的 `blowhole` 锚点,反推出「鼻孔正好露出水面 2px」时
身体中心该在哪。实测幼年体站位 y = **114.72**,和公式 84 + (0.82−0.5)×96 对到小数点后两位。

**这条的价值不在数值准,在于宪法五**:以后童童的手稿换上去、鼻孔位置挪了,
她站的高度自动跟着变,**不用改一行代码**。写死一个 0.3×worldHeight 就没有这个性质。

### 3. 热区必须跟着俯仰转 —— 这条差点被漏掉

站起来换气的时候她是 64 宽 192 高。要是还按平躺的 192×64 摆热区,
**「点她帮她换气」这条全游戏最要紧的交互就点不着了**。
实测平躺 96×56 → 站姿 56×96,转过来了。

这是那种「功能全对、但用户点不到」的坑。看代码看不出来,只有真的去点才发现。

### 4. `restY`:换气前在哪一层,换完回哪一层

不记的话,她每次换完气都会平白无故沉回默认深度。二维之后这个就很刺眼了 ——
你刚把她带到水面附近,她换个气自己跑回中层去了。实测 170.0 → 换气 → 170.0。

---

## 五、两件我不替你决定的事

### 1. 精灵开始任意角度旋转了,这是个像素画取舍

以前她只有 bob 那 ±2°。现在俯仰最大到 90°。
我放大看过成图 —— 最近邻、无抗锯齿,边缘是干净的像素阶梯,不糊。

但要说清楚:**Stardew 是不做任意角度精灵旋转的**,它宁可多画几张图。
旋转会让精灵自己的像素格和屏幕像素格错开,那个阶梯感是躲不掉的。
我的判断是这里可以接受(她本来就在水里飘,而且原本就有 2° 的转),
**但这是美术方向,该你拍板,不是我。** 不接受的话,替代方案是出几张固定角度的精灵图去切。

### 2. 对话面板开着的时候,舞台下三分之一点不到

我量过:面板占了 canvas 的 y 182–270,大约 33%。
一维的时候无所谓 —— 左右哪儿都能点。二维之后这一带就是「她能游但你点不到」的区域,
而她的默认深度 249.2 正好在里面。

面板收起来的时候下面是通的(只有右下角那个道具栏)。所以这不是天天遇得到,
但值得你知道。**我没动它** —— 你明确说过按钮尺寸那批留到统一做 UI 的阶段,我不去碰。

---

## 六、结果

| 项 | 结果 |
|---|---|
| 斜着游合速度 | **90.0 px/s**(不是 127);vx/vy 比 2.53 = 目标方向比 |
| 四个象限的俯仰符号 | 右下 / 右上 / 左下 / 竖直站姿,逐个截图看过,没有搞反的 |
| 站姿高度 | y=114.72,和锚点公式一致到小数点后两位 |
| 热区跟着转 | 平躺 96×56 → 站姿 56×96 |
| 换气回原深度 | 170.0 → 170.0 |
| 换气中横向可动、纵向锁死 | 横向 110px / 纵向 0.0px |
| `prefers-reduced-motion` | 俯仰每帧精确等于方向角 0.3709,零缓动;停下回正 |
| 控制台报错 | **全程 0** |
| `npm run build` | 通过 |
| `npm run lint` | **24 problems**,和阶段 5 收工时一模一样,改的四个文件零问题 |

**没有 commit。** 改动在分支 `pixel-ui-2026-08-04` 的工作区里,你看过再说。

---

## 七、一句话总结

这一轮真正花时间的不是代码,是**分清「哪些异常是被测对象的,哪些是测量装置的」**。
NUL 字节、位置漂移,两次都是测量装置在骗我,两次都是「多比一步」才拆穿的 ——
一次是跟 HEAD 比,一次是跟输入来源比。
代码本身其实一次就写对了。
