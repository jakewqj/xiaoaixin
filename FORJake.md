# FORJake · S2 收工:2-6 实测 + 2-7 + 2-8(2026-08-24)

> 上一份复盘(点哪游哪改成二维,2026-08-06)在 git 历史里。
> 这一轮最值得说的一件事:**你那句「必须看截图调」救了整个 2-6。**

---

## 一、你怀疑坐标是拍脑袋的 —— 比你想的还糟

你说的是「y=162 和 y=52 得看截图调一调」。实际情况是**三处全错,而且错的量级不是「差几像素」**:

**第一处:横坐标漏了除以 100。** 海草的 `x` 字段是 0–100 的百分比,渲染海草的地方老老实实写了
`(plant.x / 100) * STAGE_WIDTH`,而摆牌子的地方写成了 `plant.x * STAGE_WIDTH`。
算出来是 2880 到 45120,而整个世界一共 1440 宽。

也就是说:**那四块海草名牌从来就没进过画面。** 上一轮报告里写的「代码完成」是真的,
写的每一行都在,只是那东西被扔到了世界外面几十屏的地方。任何不看画面的检查都发现不了 ——
存档对、热区数组生成了、React 没报错。

**第二处:两个坐标系搞混了。** 这个更隐蔽。舞台是 480×270,但**世界比舞台高**:
`worldHeight = 270 + 86`,多出来的 86 是水面线以上的天空(换气时镜头摇上去才看得见)。
牌子的坐标活在世界里,而上一轮写的是 `STAGE_HEIGHT - 108` —— 用舞台的高度去算世界里的位置。
结果 162 换算过去正好是水体中央,截图里四块木牌像画框一样挂在半空。
地点牌的 `y = 52` 更远:那是水面线**以上**,平时根本不在画面里。

**第三处:地点牌钉在每格左边 20px,而那儿永远是珊瑚堆。** 三个装饰变体的左右两角
(0–110 和 360–480)都塞满了岩石珊瑚。关键是:**装饰和牌一样是世界坐标钉死的**,
不是镜头晃一下就好 —— 那块牌永远被埋掉大半。

### 可迁移的一条

前两处的共同根因是**同一套几何被抄了两份**:海草长在哪、沙面在哪,渲染层
(`drawSeagrassBed`)本来就知道,而摆牌子的代码在 React 那边重新算了一遍。
抄的时候漏一个 `/100`、用错一个常量,没有任何自动检查能发现。

所以我把摆位挪回渲染层(`seagrassNoteRow` / `placeNotePos`),App 只管「有没有、写了什么」。
**几何要跟着知道几何的那个人走。**

---

## 二、顺手挖出两个老 bug,都是「时序」不是「逻辑」

这两个不在任务里,但不修的话 2-6 本身就是坏的。

**① 家海草床那块牌一整局都不会出现。**

渲染层用 `onSpotChanged` 告诉 React「她游进第几格了」。第 0 格的 `-1 → 0` 是开局第一帧发的。
问题是 `setSnapshot` 结尾会**同步**画一帧,而 React 那边 effect 的顺序是先 `setSnapshot`
后 `onSpotChanged` —— 那一帧发出去的时候,回调还没挂上;而内部的 `spotIndex` 已经落定成 0,
**永远不会再发第二次**。

这是「晚订阅的人收不到当前值」的经典形状。修法也是经典的:订阅时补发一次。

顺带 React 那边也改了:第一帧发出来的时候 `config.json` / `world.json` 多半还没回来,
当场查地点表也是查不到的 —— 所以改成「先记下格号,等表加载完再对」。
**一个信号有两处会丢,两处都得堵。**

**② `useConfig` 每次渲染都返回一个新的 config 对象。**

上一轮特地给 `allSpots` / `openSpots` 加了 `useMemo`,理由写得清清楚楚。
但它们的依赖是 `config`,而 `config` 每次渲染都是 `mergeConfig()` 现产的新对象 ——
**那些 memo 全部被架空了,一天都没生效过。**

平时这只是白算几下,看不出来。这次我在依赖链上加了一个 `setState`,当场就是
`Maximum update depth exceeded` 刷屏。

可迁移的一条:**memo 的价值取决于它依赖的东西稳不稳。** 加 memo 的时候顺着依赖链往上看一层,
上游是现产的对象,你这一层就是装饰品。

---

## 三、你拍板的那个取舍,数据是这样的

海草名牌满了会连成一道栅栏 —— 我没有直接改,是先造了 12 棵种满的场景截了图给你看。
截图里最明显的不是「乱」,是**「小尾巴」被「家」盖掉了半块**。

这就是为什么我停下来问你而不是自己定:盖住的是童童写的字,那是原则 10 的红线,
而三种解法(只给写过的挂 / 撞了往上叠 / 接受重叠)对她的体验差别很大。
你选了第一个,代价是点了「先不写」就补不了名字 —— 这个代价我在选项里写明了。

配套我还是加了一道躲让(后摆的牌先试右边、右边占了翻左边、两边都占才往右排),
因为「只给写过的挂」并不能保证不重叠:两棵挨得近的海草都写了名字,照样撞。
**你选项里写着「她的字永远不会被别的牌压住」,那我就得让它真的成立。**

---

## 四、又一次:数字全绿,看图才发现不对

上一轮的教训是「挂画缩略图必须开 `imageSmoothingEnabled`,否则格子是纯白的,
而木色像素数一直是 372」。这一轮换了个形状又来一次:

空木牌的凹槽原来是深棕(64,40,22)。功能上完全正确 —— 它就是「还没写」的样子。
但在亮沙地上,它是**整屏最重的一块**,一排下去像几个黑窟窿,眼睛全被吸过去。

改成纸色(255,253,246,和导出时垫的底同一个值)之后,没写的牌读起来是「一张空白的纸」,
写过的牌那张画正好铺满凹槽、亮度不跳。

**这条没有任何断言能写。** 我能写的只有「牌在不在、坐标对不对」,而它一直是对的。

---

## 五、2-8 里一个不显眼但会咬人的决定

「标出每张的来处」看起来可以从 `hangings` / `notes` / `cardDrawings` 反查 —— 三张表都在。

但 2-4 有一条规则:每个邻居最多挂 3 张,满了最旧那张**从墙上挪走但不删**,它还在相册里。
挪走之后,那张画在三张表里都查不到了 —— 而相册还得说得出它是送给谁的。

所以来处必须在**画下来的那一刻**单独记一条(`drawingOrigins`)。
存的是 key(`npc:dolly`)不是现成的句子,这样将来邻居改了名字,相册里跟着改。

可迁移的一条:**「从现有数据反推」在有淘汰机制的地方一定会断。** 反推之前先问:
这条数据会不会被移走?会的话就得在发生的当下记。

---

## 六、测试环境:种存档要等 app 先稳定下来

这个坑让我第一次跑过、第二次跑挂,查了一会儿才反应过来是竞态。

流程是:打开页面 → 写 localStorage 种子 → 刷新。问题在中间那步:
app mount 之后自己还会写好几次存档(config 加载完、`visited` 记一笔……),
如果种子写在那些写入之前,就被 app 内存里那份全新存档原样盖掉了。

修法很土:开头先 `sleep(2500)` 让它写完,再种。
**判断「测试挂了是代码问题还是环境问题」,先看它上一次跑过没有。**

---

## 七、你说「接上」,于是接了 —— 但接之前得先拆一个雷

`系统开关` 里三个开关(图鉴 / 画画送礼 / 手写命名)从来没人读过。接上很简单,
真正要想清楚的是另外两件事。

**雷一:那两个开关在 `config.json` 里是 `false`。**

照原样接上去 = 上线当天 2-4 和 2-6 全部消失。那个文件的 `_当前状态` 写着「S1 目标状态」——
默认值是 S1 时代定的,那会儿这两个系统还不存在,写 `false` 是对的。现在 S2 做完了,
它们就该是 `true`。所以我一并改了,并把 `_当前状态` 改成 S2。

可迁移的一条:**接一个从没被读过的开关之前,先看它现在是什么值。**
「把它接上」和「按它现在的值执行」是两件事,后者可能是灾难。

**雷二:关掉之后,她已经画过写过的东西怎么办?**

我定的是 **关掉 = 不再出新的,不是抹掉已有的**。你关掉「画画送礼」,
Dolly 墙上那张画照旧挂着;你关掉「手写命名」,她写过名字的木牌照旧立着。
只有「能不能再来一张」这件事被关掉了。

理由是原则 10。你在后台拨一下,童童第二天打开发现自己画的画从墙上没了 ——
这跟删掉它没区别,她不知道那是「被关掉」还是「弄丢了」。

顺带一个小决定:图鉴关掉之后那一格是**空的**,不是灰格子配「长大了才有」。
后者是句假话 —— 它不是没长大,是你关了。这个项目里骗她的代价太高。

**顺手修掉一个几百毫秒的窗口,但后果是原则 10。**

改的时候发现木牌原来靠 `src` 是不是 null 判「写没写过」。而图是从 IndexedDB 异步读的:
**读到之前 `src` 就是 null,写过的牌在那一小段时间里是一块可点的空牌 ——
点下去就把她写的字覆盖掉了。** 现在 `written` 和 `src` 分开,判据换成前者。

这种 bug 手动测几乎撞不上(窗口太短),但它一定会在某个下午发生一次。
可迁移的一条:**「有没有」和「读出来没有」是两个状态,别用同一个字段表示。**

---

## 八、图鉴手绘页也单开了一个键

你说要,那就加了「图鉴手绘页」,排在「图鉴」后面。

没并进「画画送礼」的理由还是那个:那个开关管的是「送给邻居」那条路。
你关掉它是不想让她再往 Dolly 墙上挂东西,不见得是不想让她在图鉴上画画。
一个开关只该管一件事,不然你哪天想只关一半就没得关了。

**关掉之后有个小选择**:没画过的那张卡,左半是留一块空框、还是整块不出现?
我选了后者 —— 那张卡回到纯文字,读起来像本来就没有手绘页这回事。
留一块空框等于在她面前摆一个够不着的东西,那是另一种形式的死控件。
画过的那几张照旧贴着,「你画了 N 张」也照旧在。

顺带确认了一件事:图鉴整个关掉的时候,手绘页自然也进不去 —— 因为**没有入口**,
不是留一个点不动的按钮。两级开关叠在一起不会漏出一个坏状态。
