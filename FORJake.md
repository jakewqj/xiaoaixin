# FORJake · 整个世界搬进 Canvas(2026-08-04)

> 这一轮把 `REFACTOR_PLAN` 的阶段 1 和阶段 2 一起做完了:背景六层 + 角色层进 canvas,
> 小爱心的位移和镜头搬出 React。上一轮(像素 UI 收口)的复盘在 git 历史里,commit 605754a。

---

## 一、先说三处我自己改了计划的地方

计划写得很细,但有三条我没照做。都是当场决定的,理由写在这里,你不同意可以推翻。

### 1. 没建 `?canvas=1` 双轨,直接切过去了

计划里双轨的作用是「出问题能二分定位」。但双轨要求 `usePetSwim` 继续活着 —— 而它每帧
`setX` 一次,即每帧触发一次 React 全树重渲染。**留着它,这个阶段最大的一项收益当场作废。**
React 的 hook 规则又不允许「DOM 模式才调这个 hook」,绕开它得多写一个二十来个 prop 的
包装组件,养两套渲染路径。

我换了个办法拿回二分能力:**动手之前先把旧渲染的画面截成基线存下来**,改完拿新画面跟它比。
真出问题,`git checkout 605754a` 就是完整的旧版本。空间上的 A/B 换成时间上的 A/B,
既没丢定位能力,也没养两套代码。

### 2. 顺手做掉了阶段 4 里「拆旧渲染」的世界那一半

HUD 那一半 8 月 3 号那轮已经拆了。这次把 `Scene.tsx` 那一半也拆掉之后,阶段 4 就只剩
「去掉双轨开关」——而双轨我压根没建。所以阶段 4 是被自然消化掉的,不是我抢跑。

### 3. `prefers-reduced-motion` 提前实现了(原定阶段 4)

这条没得选。世界层一离开 DOM,`index.css` 里那条媒体查询就管不着它了 —— 不当场实现,
「减弱动态效果」这个已经交付的功能会**直接倒退成没有**。现在走 `WorldRenderer` 的运行时开关。

有个例外我特意保住了:**换气泡泡照旧画出来,只是不动。** 这条是从 `index.css` 里
一行注释继承下来的 —— 泡泡是「该换气了」的唯一提示,一起关掉她就永远不知道该点。

---

## 二、方法:先建基线,再动手

这轮真正花时间的不是写渲染代码,是**建立一把能量化「画面有没有变」的尺子**。

一句「看起来一样」值不了多少钱。所以流程是:

1. 动手前,用无头 Chrome + CDP 把旧渲染的两个画面截下来,存成基线
2. 写新渲染
3. 截同样两个画面,逐像素比,把差异标成品红色输出成一张图
4. 对每一块差异区域扫 dx/dy,判断是「整体偏了几像素」还是「只是抗锯齿不一样」

### 难点:两边怎么停在同一时刻

画面里全是动画 —— 云在飘、水面在漂、海带在摇、精灵在翻帧。不冻住就没法比。

**DOM 那边**:用 CDP 的 `Animation.seekAnimations` 把所有 CSS 动画 seek 到 `currentTime = 0`。
妙处在于项目里的动画大量用负 delay(`animationDelay: -7s`),seek 到 0 之后它们会停在
`|delay|` 那个进度上 —— 是个**确定值**,不随截图时间变。

**canvas 那边**:加一个开发构建限定的 `window.__renderFreeze(0)`,把主循环时钟钉死在 0。
我的相位公式吃的就是同一个 `now`,算出来自然是同一个进度。

验证这把尺子本身:连截两次基线,逐像素比,**0/129600 不同**。尺子是准的,可以拿它量东西了。

### 结果

| | 差异 | 最吻合的位移 |
|---|---|---|
| 家海草床 | 861 / 129600 = **0.664%** | dx=0 dy=0 |
| 海豚潟湖 | 529 / 129600 = **0.408%** | dx=0 dy=0 |

**逐块扫下来,没有任何一处偏了哪怕 1px。** 剩下的 0.4–0.7% 全是光栅化差异:
旋转的精灵(bob 的 ±2°、海带摇摆)CSS 走合成器抗锯齿、canvas 走最近邻;
海草床本来就是矢量描边;影子是 120px 的图放大到 170px,非整数。这几样想追平得重写渲染器,
不值得,而且肉眼看不出来。

---

## 三、逮到的两个坑,都是「CSS 默认值和直觉不一样」

这两个**光读代码不可能发现**。我第一遍翻译的时候,两处都写得理直气壮。

### 坑 1:`background-position: bottom` 的横向不是 0,是 50%

水下那层深色剪影,原来的写法是:

```js
backgroundImage: `url(far.png)`,
backgroundRepeat: 'repeat-x',
backgroundPosition: 'bottom',
```

我照着翻译成「从 x=0 开始横向平铺」。截图一比,整层错了 **120px**。

原因:`background-position: bottom` 是 `50% 100%` 的简写 —— 你只想说「贴底」,
但它同时把**横向**设成了 50%。而背景的百分比定位不是「容器的 50%」,是
`(容器宽 - 图宽) × 50%`。世界宽 1440、图宽 240,起点被挪到了 600,600 % 240 = 120。

**可迁移的经验**:CSS 的简写属性会顺手把你没提到的那一半也设掉。`background-position`、
`background`、`font`、`flex` 都是这样。翻译一段 CSS 到别的渲染系统时,
简写属性要先展开成完整形式再动手。

### 坑 2:`<button>` 会把内容垂直居中

Dolly 的精灵是 112×48,装在一个带 `min-h-14`(56px 触摸下限)的按钮里。
我按「按钮是 block,内容从顶上排」翻译,结果她整体高了 **4px**。

真相是 Blink 对 `<button>` 有固有行为:**按钮比内容高时,内容上下居中**。
这不是哪条 CSS 写的,是渲染引擎内建的。多出来的 8px 上下各分 4px —— 正好对上。

改成居中之后,Dolly 那块区域的差异是 **0.00%**,一个像素都不差。

**可迁移的经验**:表单元素(`button` `input` `select` `textarea`)有一堆 UA 内建的
布局行为,不出现在任何 stylesheet 里。碰到它们时别推理,量。

### 顺带一提:这两个坑本来会永远藏着

它们只在「把 DOM 翻译成 canvas」的时候才暴露。如果我只是「看起来差不多就收工」,
剪影错 120px 和 Dolly 高 4px 都会当成新画面的一部分留下来 —— 而且以后再也没人会发现。
**这就是为什么值得先花时间建那把尺子。**

---

## 四、这轮真正的收益,和它为什么值这个价

| 指标 | 之前 | 现在 |
|---|---|---|
| 游动时 App 重渲染 | 60 次/秒 | **0 次** |
| 同时跑动画的 DOM 元素 | 每个地点格约 32 个,6 格约 190 | 12 个透明热区(不绘制) |
| 巡游速度 | 90 逻辑px/秒 | 90.1(算法原样搬,没走样) |

`usePetSwim` 那个 hook 是整个项目最贵的一处:`setX` 在 rAF 里每帧调一次,
React 就每帧把 App 整棵树重算一遍 —— 对话、HUD 快照、NPC 列表、海草床,全部。
一秒 60 遍。**iPad 上六个地点全开会掉到 30–40 帧,主要就是它。**

搬走它之所以可行,是因为接缝比想象中干净:`petSwim.x` 只有三个消费者(镜头、影子、
小爱心本体),重构后三个全在 canvas 里。**没有任何 React 组件还需要知道她游到哪。**
这也是「State 零改动」能成立的关键 —— `src/hooks/` 全程只读,`SaveData` 一个字段都没加。

---

## 五、取舍清单

**背景每帧都重画,没做脏检查。** 计划里写了「镜头没动且摇摆没到下一帧时跳过重绘」。
但海带摇摆是连续的,几乎每帧都变,脏检查基本永远为真,写了也白写。
480×270 上四十来次 `drawImage` 便宜得可以忽略,真正的收益在于砍掉了 190 个动画 DOM
和 60 次/秒的 React 重渲染。留了个笨办法:开了减弱动态效果时跳过。

**体色染色改成预染缓存。** 原来是 CSS `filter`,每帧交给合成器。canvas 每帧
`ctx.filter` 太贵,改成整张精灵表预染一次存起来 —— 体色只在成长阶段变化时才动,
一辈子几次。

**海草床是唯一一处不是 `drawImage` 的绘制。** 它本来就是 SVG 矢量描边,烧成 PNG 反而失真。
翻译时踩到一个细节:SVG 默认 `preserveAspectRatio="xMidYMid meet"` 是**等比缩放后居中**,
不是拉伸 —— 按拉伸写形状会明显变形。

**没建 `overlay` 那块 canvas。** 计划里是四块,我只建了两块。第三块(前景遮挡/光照)
是阶段 5 的东西,现在建就是一块什么都不画的空图层,白占一层合成。

---

## 六、测试上踩的坑(不是代码 bug,但浪费了我几轮)

**自动化测试的点击会误伤真实的游戏功能。** 我写的「一路点到潟湖」循环,点在舞台 (470,120),
半路正好砸在 Dolly 的送礼格上 —— 礼物在正式测试送礼之前就送掉了,然后送礼那条断言红了,
我一度以为是热区坏了。

后来把点击位置挪到 (340,150),避开小爱心本体、海草床、HUD 快捷栏和送礼格。
**教训:在一个到处是热区的界面上做自动化,「点哪」要当成一个需要论证的参数,不能随手拍。**

另一个:点到 Dolly 会弹出对话,而那一下不会让小爱心移动,循环就白跑一次。
所以循环里加了「如果弹了对话就先关掉再继续」。

---

## 七、下一步

只剩 `REFACTOR_PLAN` 的阶段 5:视差、水下光柱、前景遮挡礁石、镜头 easing。
**这一阶段才「变好看」**,而且允许画面变化 —— 所以前四个阶段那把「逐像素比」的尺子
到这里就不能当验收标准了,改成每做一项停下来给你看。

那把尺子本身别扔:改完视差之后,关掉视差因子应该还能比回今天这个基线。
