# FORJake · 真美术落地这一轮

## 我做了什么、为什么这么做

这轮目标是把世界从"纯色占位"推到参考图那种 Stardew 质感,外加重画小爱心。我把它拆成四刀:重画精灵 → 生成世界素材 → 接进 Scene → 统一剩余 UI,最后用无头浏览器实测。

关键决定是**继续走程序化生成像素画**这条路(`draw-world.mjs` 跟 `draw-pet.mjs` 一个思路),而不是让你去切参考图或者找 AI 生图。原因有三:

1. 参考图是 AI 生成的伪像素,像素网格不齐,直接切出来的素材缩放后会糊;
2. 程序化生成的素材天然满足"16 色内、无抗锯齿、可复现"这三条规范,每次跑出来一模一样;
3. 以后想调一个珊瑚的颜色,改一行代码重跑就行,不用重新做图。

代价是单张素材的精细度上限比手绘低。但在 480×270 的逻辑分辨率下(单个珊瑚就 24×16 像素),这个上限够用了——你从截图能看出来,整体氛围已经到参考图的味道了。

## 考虑过的替代方案

- **切参考图当素材**:ROADMAP 第 6 节其实建议过这条路。放弃原因如上——伪像素切不干净,而且切图这活儿没法"只往文件夹拖 4 个 PNG 不改代码"地迭代。
- **给每片海各做一套珊瑚/沙底**:没做。远景剪影我刻意做成了**半透明深色**,叠在哪片海的水色渐变上都成立,所以换海只换渐变就有区分度。三套素材的差异化留给以后真的需要时再做。
- **装 Playwright 做验收**:项目宪法说依赖能不装就不装。机器上正好有 Chrome,Node 22 自带 WebSocket,所以我手写了个 80 行的 CDP 驱动脚本(在 scratchpad 里,没进仓库),点按钮、摇镜头、截图全干了。

## 做出的取舍

- **小爱心从冷蓝灰换成暖灰褐**:参考图的儒艮就是暖调的,而且真儒艮也偏棕灰。CLAUDE.md 的 `--dugong-body #8CA3B0` 还留着(CSS 变量没删),但精灵 PNG 用的是新调色板。这是你 goal 第 3 条授权的覆盖。
- **水面线从 86.4px 取整到 86px**:0.4px 的差别肉眼看不出,但背景 tile 是整数像素画的,非整数高度会引起半像素拉伸——宁可动布局常数不动像素。
- **Dolly 的重叠问题用"高度差"解决而不是"挪左边"**:先试了往左挪,结果撞进小爱心的漂移带(她横向漂 ±40px,占掉画面中间一大段)。抬高到中上层水域一石二鸟:海豚习性合理,而且以后再加 NPC 也有了个准则——底栖的走沙面,游泳的走上层。

## 遇到的坑

1. **CDP 脚本第一轮全程空跑**:我用 `textContent.includes('你好')` 找开场白的选项按钮,结果先匹配到台词区(「你好呀」也含「你好」),点了没反应,后面 7 张截图全是同一个画面。教训:验收截图必须逐张目检,"脚本跑完没报错"什么都不证明。
2. **胡须点的散布公式一开始是 `(x*3+y*7)%11`**,出来的不是"散点"而是整齐的斜线纹,像刮痕。换成互质更大的系数才像随机。程序化生成里,"看起来随机"比"真随机"难。
3. **儒艮大婶是踩着小爱心的坐标写的**(疤痕、张嘴的具体像素位置)。重画小爱心等于把她脚下的地基换了。这次是我记得去重跑+调整,但这个耦合还在——以后再动 `draw-pet.mjs` 的轮廓曲线,记得 `draw-dugong-aunt.mjs` 要跟着看一眼。

## 可迁移的经验

- **半透明剪影是廉价的差异化杠杆**:一张远景 tile 配三种水色渐变 = 三片海的观感,素材成本 1/3。UI 或数据可视化里同理——想让一套素材适配多主题,把它做成"只有形状没有固有色"。
- **确定性伪随机(hash 坐标)比 `Math.random()` 适合生成美术**:同一份代码永远出同一张图,diff 干净,review 有意义。
- **无依赖 CDP 驱动**这招值得记住:Node ≥21 自带 WebSocket,`/json/list` 拿到 ws 地址,`Runtime.evaluate` + `Page.captureScreenshot` 两个方法就能做完 80% 的浏览器自动化,不用给项目装任何东西。
